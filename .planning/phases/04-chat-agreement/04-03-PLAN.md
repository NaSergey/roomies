---
phase: 04-chat-agreement
plan: 03
type: execute
wave: 3
depends_on:
  - "04-02"
files_modified:
  - "front/widgets/chat/ui/MatchList.tsx"
  - "front/widgets/chat/ui/MessageBubble.tsx"
  - "front/widgets/chat/ui/SmartChips.tsx"
  - "front/widgets/chat/ui/ChatConversation.tsx"
  - "front/widgets/chat/ui/ChatView.tsx"
  - "front/widgets/chat/index.ts"
autonomous: true
requirements:
  - CHAT-01
  - CHAT-02
  - CHAT-03
  - CHAT-06

must_haves:
  truths:
    - "ChatView shows MatchList by default (no match selected)"
    - "Tapping a match row transitions to ChatConversation for that chat (per D-04)"
    - "No Next.js route changes — local state only (per D-05)"
    - "MatchList shows loading skeleton, empty state, and match rows with avatar/name/last message/time/unread badge"
    - "ChatConversation polls for new messages every 3 seconds (per D-02)"
    - "On mount, ChatConversation calls markChatRead once"
    - "Empty chat shows SmartChips with 5 starter questions (per D-10)"
    - "SmartChips disappear once messages array is non-empty"
    - "Nudge bar appears when messages.length > 0 AND newest message is more than 12h old (per D-11)"
    - "Tapping Напомнить sends POST /chats/:chatId/messages with hardcoded nudge text"
    - "Telegram BackButton is activated in ChatConversation and deactivated on unmount (per D-04)"
    - "MessageBubble renders own messages right-aligned (accent bg), partner messages left-aligned (card bg)"
    - "system type messages are centered gray italic"
    - "call_invite type messages render placeholder slot for CallInviteCard (passed as prop from ChatConversation)"
    - "Input area has text field, send button, plus 📞 and 🤝 icon buttons (sheets wired in 04-04)"
  artifacts:
    - path: "front/widgets/chat/ui/MatchList.tsx"
      provides: "Match list with loading/empty states and row tap handler"
    - path: "front/widgets/chat/ui/MessageBubble.tsx"
      provides: "Single message renderer for text/system/call_invite types"
    - path: "front/widgets/chat/ui/SmartChips.tsx"
      provides: "5 starter chip row, visible only when 0 messages"
    - path: "front/widgets/chat/ui/ChatConversation.tsx"
      provides: "Full chat view with polling, markRead, nudge, input area"
    - path: "front/widgets/chat/ui/ChatView.tsx"
      provides: "Root chat tab component — local state routing between MatchList and ChatConversation"
  key_links:
    - from: "front/widgets/chat/ui/ChatView.tsx"
      to: "front/widgets/chat/ui/MatchList.tsx"
      via: "onSelect(chatId, partnerId, partnerName, partnerPhoto) callback → setSelected"
      pattern: "onSelect"
    - from: "front/widgets/chat/ui/ChatConversation.tsx"
      to: "front/features/chat"
      via: "useChatMessagesQuery(chatId), useSendMessage(chatId), useMarkChatRead(chatId)"
      pattern: "useChatMessagesQuery|useSendMessage|useMarkChatRead"
    - from: "front/widgets/chat/ui/ChatConversation.tsx"
      to: "front/shared/lib/telegram"
      via: "getWebApp().BackButton.show() / hide() in useEffect"
      pattern: "BackButton"
---

<objective>
Replace the ChatView stub with a fully functional MatchList + ChatConversation navigation flow. Build all 5 UI components: MatchList, MessageBubble, SmartChips, ChatConversation, and the updated ChatView state machine.

Purpose: Delivers CHAT-01 (match list), CHAT-02 (smart chips), CHAT-03 (send messages), and CHAT-06 (nudge bar) end-to-end. After this plan, the Chat tab is fully interactive for text messaging.
Output: 5 component files replacing the ChatView stub. CallInviteCard and AgreementCard rendering is wired in 04-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-chat-agreement/04-CONTEXT.md
@.planning/phases/04-chat-agreement/04-02-SUMMARY.md

<interfaces>
<!-- Key types and patterns the executor needs. Extracted from codebase. -->

From front/features/chat/index.ts (created in 04-02) — hooks available:
```typescript
import {
  useMatchesQuery,       // staleTime: 30_000
  useChatMessagesQuery,  // refetchInterval: 3000
  useSendMessage,        // mutate(content: string)
  useMarkChatRead,       // mutate()
  chatKeys,
} from '@/features/chat';
```

From front/shared/lib/api (created in 04-02) — types needed:
```typescript
import type { MatchListItem, ChatMessage } from '@/shared/lib/api';

// MatchListItem:
// { matchId, chatId, partner: { id, name, photo: string|null }, lastMessage: { content, createdAt, senderId, messageType }|null, unreadCount, matchScore }

// ChatMessage:
// { id: string, chatId, senderId, content: string|null, messageType: 'text'|'system'|'call_invite', metadata: Record<string,unknown>|null, createdAt: string }
```

From front/shared/lib/telegram/index.ts — BackButton pattern:
```typescript
import { getWebApp } from '@/shared/lib/telegram';
// Usage in useEffect:
// const wa = getWebApp();
// wa?.BackButton.show();
// wa?.BackButton.onClick(handler);
// cleanup: wa?.BackButton.offClick(handler); wa?.BackButton.hide();
```

From front/widgets/swipe-deck/ui/FilterSheet.tsx — bottom sheet animation pattern:
```typescript
// Overlay: fixed inset-0 z-30 bg-black/40 backdrop-blur-sm transition-opacity
// Sheet: fixed inset-x-0 bottom-0 z-40 rounded-t-3xl border-t-2 border-black bg-white transition-transform duration-300
// Open: translate-y-0  Closed: translate-y-full
```

From front/widgets/home/ui/HomeView.tsx — local state navigation pattern:
```typescript
const [tab, setTab] = useState<NavTab>('deck');
// ChatView uses same pattern internally:
// const [selected, setSelected] = useState<SelectedChat | null>(null);
// when null → show MatchList; when set → show ChatConversation
```

From front/app/globals.css color tokens used in existing components:
```
var(--text)       → primary text
var(--text-muted) → secondary / muted text (also class: text-muted in Tailwind config)
var(--bg)         → background
--card            → card background (used for partner bubble)
bg-accent         → accent green (used for own bubbles, active chips, send button)
```

Existing ChatView stub (to replace body, keep export name and 'use client'):
```typescript
'use client';
export function ChatView() {
  return (
    <div className="flex flex-1 flex-col items-center justify-center gap-3 px-6 text-center">
      <span className="text-5xl" aria-hidden>💬</span>
      // ...
    </div>
  );
}
```

Nudge rule (per D-11): messages.length > 0 AND new Date().getTime() - new Date(messages[0].createdAt).getTime() > 12 * 60 * 60 * 1000 (messages[0] is the newest — backend returns newest first, frontend reverses for display but original array[0] is newest).

Message display order: backend returns newest first. ChatConversation should display messages chronologically (oldest at top, newest at bottom) — reverse the array before rendering. The nudge check uses messages[0] from the raw API response (newest).

Smart chips (per D-10):
```typescript
const SMART_CHIPS = [
  'Как у тебя с гостями?',
  'Тишина после 23:00?',
  'Как насчёт питомцев?',
  'Работаешь из дома?',
  'Какой у тебя режим сна?',
];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MatchList, MessageBubble, SmartChips</name>
  <files>
    front/widgets/chat/ui/MatchList.tsx
    front/widgets/chat/ui/MessageBubble.tsx
    front/widgets/chat/ui/SmartChips.tsx
  </files>
  <read_first>
    - front/widgets/swipe-deck/ui/FilterSheet.tsx (Chip component pattern, color classes)
    - front/app/globals.css (color tokens: --text, --card, --bg, text-muted, bg-accent)
  </read_first>
  <action>
    Create front/widgets/chat/ui/MatchList.tsx:
    - 'use client' directive at top
    - Props interface: MatchListProps { onSelect: (chatId: number, partnerId: number, partnerName: string, partnerPhoto: string | null) => void }
    - Import useMatchesQuery from '@/features/chat'; import MatchListItem from '@/shared/lib/api'
    - Call useMatchesQuery(); destructure { data: matches, isPending }
    - Loading state (isPending): render 3 skeleton rows — each a div with animate-pulse, h-16, rounded-xl, bg-[#f0efe9]
    - Empty state (!matches || matches.length === 0): centered div with 💬 emoji and text "Здесь появятся переписки с мэтчами."
    - Match rows: scrollable list (div className="flex flex-col gap-0 overflow-y-auto flex-1"). Each row is a button element with onClick calling onSelect.
    - Row layout: flex items-center gap-3 p-3 border-b border-[#f0efe9] active:bg-[#f8f8f5]
    - Avatar: 48x48 rounded-full bg-accent flex items-center justify-center. If partner.photo → img element with src, alt, rounded-full object-cover w-12 h-12. If no photo → span showing first letter of partner.name (uppercase, font-black text-[#14140f])
    - Right side: flex flex-col flex-1 min-w-0 gap-0.5
      - Top row: flex justify-between items-baseline → span partner.name (font-black text-sm text-(--text)) + span time (text-xs text-muted)
      - Bottom row: flex justify-between items-center → span last message text truncated (text-xs text-muted truncate flex-1 mr-2), unread badge
    - Time display: if lastMessage, format createdAt — if same calendar day show time ("14:32") via Date.toLocaleTimeString('ru-RU', { hour: '2-digit', minute: '2-digit' }), else show "вчера" for yesterday or date string. Simple check: new Date(createdAt).toDateString() === new Date().toDateString() → show time, else → "вчера" (for MVP, a simple comparison is fine).
    - Last message text: if lastMessage?.content, truncate to 1 line. If messageType is 'call_invite' → show "📞 Предложение созвониться". If messageType is 'system' → show "🤝 Соглашение". If content is null → ""
    - Unread badge: if unreadCount > 0 → span className="ml-auto shrink-0 flex items-center justify-center w-5 h-5 rounded-full bg-rose-500 text-white text-[10px] font-black" showing unreadCount (cap display at 99+). Color: bg-[#ff6b6b] (or bg-rose-500 if available via Tailwind).

    Create front/widgets/chat/ui/MessageBubble.tsx:
    - 'use client' directive
    - Props interface: MessageBubbleProps { message: ChatMessage; isOwn: boolean; callInviteSlot?: React.ReactNode; agreementSlot?: React.ReactNode; }
    - Import ChatMessage from '@/shared/lib/api'
    - For messageType === 'system': return div className="flex justify-center my-1" with span className="text-xs italic text-muted text-center px-4" showing message.content
    - For messageType === 'call_invite': return full-width card area. If callInviteSlot provided, render it. If not, show a placeholder div with text "📞 Созвон" (04-04 replaces this).
    - For messageType === 'text' (default): render speech bubble
      - Container: div className="flex {isOwn ? 'justify-end' : 'justify-start'} mb-1"
      - Bubble: div className="{isOwn ? 'bg-accent text-[#14140f]' : 'bg-[var(--card,#f5f5f0)] text-(--text)'} max-w-[75%] rounded-2xl {isOwn ? 'rounded-br-sm' : 'rounded-bl-sm'} px-3 py-2"
      - Content: p className="text-sm leading-snug break-words" showing message.content
      - Timestamp: div className="text-[10px] text-right mt-0.5 opacity-60" showing new Date(message.createdAt).toLocaleTimeString('ru-RU', { hour: '2-digit', minute: '2-digit' })
    - For agreementSlot: if messageType === 'system' AND message.metadata?.agreementId AND agreementSlot is provided → render agreementSlot instead of the centered system text

    Create front/widgets/chat/ui/SmartChips.tsx:
    - 'use client' directive
    - Props interface: SmartChipsProps { onChipTap: (text: string) => void }
    - SMART_CHIPS constant array (5 items per D-10):
      ['Как у тебя с гостями?', 'Тишина после 23:00?', 'Как насчёт питомцев?', 'Работаешь из дома?', 'Какой у тебя режим сна?']
    - Render: div className="px-3 py-4 flex flex-col gap-3"
      - Heading: p className="text-xs text-muted font-black uppercase tracking-widest" → "Начни разговор"
      - Chips row: div className="flex flex-wrap gap-2"
      - Each chip: button type="button" onClick={() => onChipTap(text)} className="rounded-full border-2 border-black bg-white px-3 py-1.5 text-sm font-bold text-(--text) active:scale-95 transition-transform duration-100"
  </action>
  <verify>
    <automated>cd front && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript reports no errors. MatchList.tsx, MessageBubble.tsx, SmartChips.tsx exist under front/widgets/chat/ui/. MatchList renders loading skeletons when isPending. MessageBubble handles all three messageTypes.</done>
</task>

<task type="auto">
  <name>Task 2: Create ChatConversation and replace ChatView</name>
  <files>
    front/widgets/chat/ui/ChatConversation.tsx
    front/widgets/chat/ui/ChatView.tsx
    front/widgets/chat/index.ts
  </files>
  <read_first>
    - front/widgets/chat/index.ts (current barrel — ensure ChatView export is preserved)
    - front/shared/lib/telegram/use-telegram-web-app.ts (getWebApp() pattern for BackButton)
    - front/widgets/chat/ui/MessageBubble.tsx (just created — import it)
    - front/widgets/chat/ui/SmartChips.tsx (just created — import it)
  </read_first>
  <action>
    Create front/widgets/chat/ui/ChatConversation.tsx:
    - 'use client' directive
    - Props interface: ChatConversationProps { chatId: number; partnerId: number; partnerName: string; partnerPhoto: string | null; currentUserId: number; onBack: () => void; onOpenProposeCall: () => void; onOpenAgreement: () => void; callInviteSlot?: (inviteId: number) => React.ReactNode; agreementSlot?: (agreementId: number) => React.ReactNode; }
    - Imports: useEffect, useRef, useState from 'react'; useChatMessagesQuery, useSendMessage, useMarkChatRead from '@/features/chat'; getWebApp from '@/shared/lib/telegram'; MessageBubble from './MessageBubble'; SmartChips from './SmartChips'
    - Call useChatMessagesQuery(chatId) → { data }; messages = data?.messages ?? []
    - Call useSendMessage(chatId) → sendMutation
    - Call useMarkChatRead(chatId) → markReadMutation
    - Local state: inputText (string), useState('')

    BackButton wiring (useEffect with [] deps):
    ```typescript
    useEffect(() => {
      const wa = getWebApp();
      const handler = () => onBack();
      wa?.BackButton.show();
      wa?.BackButton.onClick(handler);
      return () => {
        wa?.BackButton.offClick(handler);
        wa?.BackButton.hide();
      };
    }, [onBack]);
    ```

    Mark read on mount (useEffect with [chatId] deps):
    ```typescript
    useEffect(() => {
      markReadMutation.mutate();
    }, [chatId]);
    ```

    Auto-scroll to bottom on new messages: useRef<HTMLDivElement>(null) on the messages container, useEffect watching messages.length to scroll to bottom via scrollTop = scrollHeight.

    Nudge bar logic: compute showNudge = messages.length > 0 && (new Date().getTime() - new Date(messages[0].createdAt).getTime()) > 12 * 60 * 60 * 1000. (messages[0] from raw API is newest — backend sends newest-first.)

    Display messages in chronological order: const displayMessages = [...messages].reverse()

    Layout structure:
    - Root: div className="flex flex-col h-full"
    - Header: div className="flex items-center gap-3 px-3 py-2 border-b-2 border-black"
      - Back button: button onClick={onBack} className="p-1 text-lg font-black" → "‹"
      - Avatar (same as MatchList row avatar): 36x36 rounded-full
      - Partner name: span className="font-black text-(--text)"
    - Messages area: div ref={messagesEndRef container} className="flex-1 overflow-y-auto px-3 py-2 flex flex-col"
      - If messages.length === 0: render SmartChips with onChipTap={(text) => sendMutation.mutate(text)}
      - If messages.length > 0: map displayMessages → MessageBubble for each. For each message:
        - isOwn = message.senderId === currentUserId
        - For call_invite type: callInviteSlot prop = message.metadata?.callInviteId ? callInviteSlot?.(message.metadata.callInviteId as number) : undefined
        - For system type with agreementId: agreementSlot prop = message.metadata?.agreementId ? agreementSlot?.(message.metadata.agreementId as number) : undefined
    - Nudge bar (conditional, above input): if showNudge render div className="mx-3 mb-2 rounded-xl border-2 border-black bg-[#fff9e6] px-4 py-2 flex items-center justify-between gap-2"
      - Text: span className="text-xs text-(--text)" → "Хочешь аккуратно напомнить {partnerName}?"
      - Button: button onClick={() => sendMutation.mutate('Привет! Интересно, как дела 🙂')} → "Напомнить" (accent button, small)
    - Input area: div className="px-3 py-2 border-t-2 border-black flex items-center gap-2"
      - Icon button 📞: button type="button" onClick={onOpenProposeCall} className="text-xl p-1 shrink-0"
      - Icon button 🤝: button type="button" onClick={onOpenAgreement} className="text-xl p-1 shrink-0"
      - Text input: input type="text" value={inputText} onChange={(e) => setInputText(e.target.value)} onKeyDown={(e) => { if (e.key === 'Enter' && inputText.trim()) { sendMutation.mutate(inputText.trim()); setInputText(''); } }} placeholder="Сообщение..." className="flex-1 rounded-full border-2 border-black px-4 py-2 text-sm bg-white outline-none"
      - Send button: button type="button" onClick={() => { if (inputText.trim()) { sendMutation.mutate(inputText.trim()); setInputText(''); } }} disabled={!inputText.trim() || sendMutation.isPending} className="rounded-full bg-accent border-2 border-black px-4 py-2 text-sm font-black text-[#14140f] disabled:opacity-50"  → "→"

    Replace front/widgets/chat/ui/ChatView.tsx entirely:
    - 'use client' directive
    - Import useState from 'react'; import MatchList from './MatchList'; import ChatConversation from './ChatConversation'
    - Import useTelegramAuth (or get currentUserId another way): import { useTelegramAuth } from '@/features/auth' to get the current user ID for isOwn bubble detection. Alternatively, pass currentUserId down from HomeView — but simpler is to import useProfileQuery from '@/features/profile' to get user.id.
    - Actually: import useProfileQuery from '@/features/profile'; call const { data: profile } = useProfileQuery(); currentUserId = profile?.id ?? 0
    - SelectedChat interface: { chatId: number; partnerId: number; partnerName: string; partnerPhoto: string | null }
    - useState<SelectedChat | null>(null) → selected, setSelected
    - Render:
      - When selected === null: MatchList with onSelect={(chatId, partnerId, partnerName, partnerPhoto) => setSelected({ chatId, partnerId, partnerName, partnerPhoto })}
      - When selected !== null: ChatConversation with chatId={selected.chatId}, partnerId={selected.partnerId}, partnerName={selected.partnerName}, partnerPhoto={selected.partnerPhoto}, currentUserId={currentUserId}, onBack={() => setSelected(null)}, onOpenProposeCall={() => { /* 04-04 wires sheets */ }}, onOpenAgreement={() => { /* 04-04 wires sheets */ }}
      - callInviteSlot and agreementSlot props: pass undefined for now (04-04 wires these)
    - Keep 'export function ChatView()' as the named export — index.ts already exports this name.

    Update front/widgets/chat/index.ts: verify it still exports ChatView (no change needed if file already has: export { ChatView } from './ui/ChatView'). If additional new components need to be exported from the widget barrel, add them. Do NOT break the existing ChatView export.
  </action>
  <verify>
    <automated>cd front && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>TypeScript reports no errors. ChatConversation.tsx exists with BackButton wiring, polling via useChatMessagesQuery, markRead on mount, SmartChips when empty, nudge bar logic, and input area with 📞/🤝 buttons. ChatView.tsx is replaced with local state routing. front/widgets/chat/index.ts still exports ChatView.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User text input → sendMutation | Message content from input field sent to POST /chats/:chatId/messages |
| Message metadata → UI rendering | metadata.callInviteId and metadata.agreementId are numbers from backend, cast before use |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11 | Tampering | User message input content | accept | Backend enforces @MaxLength(2000); frontend input does not pre-validate length (MVP); backend is the authoritative gate |
| T-04-12 | Information Disclosure | currentUserId from profile query | accept | Profile query returns own data only (GET /profile/me uses JWT sub); not a leak |
| T-04-13 | Tampering | metadata.callInviteId as number cast | accept | Cast is (metadata.callInviteId as number); if backend sends wrong type, component renders nothing (undefined callInviteSlot) — no crash |
| T-04-SC | Tampering | npm installs | accept | No new packages in this plan |
</threat_model>

<verification>
1. cd front && npx tsc --noEmit — exits 0, no type errors
2. front/widgets/chat/ui/MatchList.tsx, MessageBubble.tsx, SmartChips.tsx, ChatConversation.tsx exist
3. front/widgets/chat/ui/ChatView.tsx exports function ChatView() (not the old stub)
4. grep -n "BackButton" front/widgets/chat/ui/ChatConversation.tsx — returns lines showing show/hide/onClick/offClick
5. grep -n "refetchInterval\|useChatMessages" front/widgets/chat/ui/ChatConversation.tsx — confirms polling hook used
6. grep -n "markReadMutation" front/widgets/chat/ui/ChatConversation.tsx — confirms markRead called on mount
7. grep -n "SmartChips" front/widgets/chat/ui/ChatConversation.tsx — confirms SmartChips rendered when empty
8. grep -n "showNudge\|12 \* 60" front/widgets/chat/ui/ChatConversation.tsx — confirms nudge logic
9. front/widgets/chat/index.ts exports ChatView — grep "ChatView" front/widgets/chat/index.ts
</verification>

<success_criteria>
- npx tsc --noEmit passes in front/
- ChatView shows MatchList when no chat selected, ChatConversation when selected
- MatchList renders 3 skeleton rows during loading, empty state text when no matches, match rows with avatar/name/message/time/unread badge
- MessageBubble renders: own messages right/accent, partner messages left/card, system as centered italic, call_invite as slot
- SmartChips renders 5 chips, calls onChipTap when tapped
- ChatConversation: BackButton show on mount, hide on unmount; markChatRead called once on mount; SmartChips shown when messages empty; nudge bar shown when newest message is 12h+ old; input sends on Enter or send button click; 📞 and 🤝 buttons present
- TypeScript build clean
</success_criteria>

<output>
Create .planning/phases/04-chat-agreement/04-03-SUMMARY.md when done
</output>
