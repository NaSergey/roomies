---
plan: "01-03"
phase: 1
type: execute
wave: 2
depends_on: ["01-02"]
autonomous: true
files_modified:
  - "front/features/onboarding/model/types.ts"
  - "front/features/onboarding/model/use-onboarding.ts"
  - "front/features/onboarding/api/onboarding-api.ts"
  - "front/features/onboarding/ui/OnboardingFlow.tsx"
  - "front/features/onboarding/ui/steps/ScenarioStep.tsx"
  - "front/features/onboarding/ui/steps/LocationStep.tsx"
  - "front/features/onboarding/ui/steps/BudgetStep.tsx"
  - "front/features/onboarding/ui/steps/DealbreakersStep.tsx"
  - "front/features/onboarding/index.ts"
  - "front/shared/lib/api/geo.ts"
  - "front/shared/lib/api/index.ts"
  - "front/widgets/home/ui/HomeView.tsx"
requirements_addressed:
  - ONBOARD-01
  - ONBOARD-02
  - ONBOARD-03
  - ONBOARD-04
must_haves:
  truths:
    - "HomeView shows OnboardingFlow when user has not completed onboarding"
    - "HomeView shows SwipeDeck when user has completed onboarding"
    - "ScenarioStep renders 4 scenario cards; tapping one enables the CTA button"
    - "Tapping CTA on ScenarioStep calls PATCH /onboarding/scenario and advances to step 1"
    - "LocationStep loads city list from GET /geo/cities; city select enables CTA"
    - "Selecting a city loads districts from GET /geo/cities/:id/districts"
    - "BudgetStep accepts budgetMin/budgetMax numeric inputs and optional date/duration"
    - "DealbreakersStep has 3 toggles; guestsPref sub-selector appears when guests toggle is ON"
    - "Telegram BackButton shows on steps 1–3 and hides on step 0"
    - "BackButton tap decrements step without API call"
    - "On mount, useOnboarding calls GET /onboarding/status and hydrates state if step > 0"
  artifacts:
    - path: "front/features/onboarding/model/types.ts"
      provides: "OnboardingState, OnboardingAction, OnboardingAnswers, OnboardingStatus, payload types"
      exports: ["OnboardingState", "OnboardingAction", "OnboardingAnswers", "OnboardingStatus", "ScenarioType", "GuestsPreference", "QuizAnswer"]
    - path: "front/features/onboarding/model/use-onboarding.ts"
      provides: "useReducer state machine with BackButton side effect and resume detection"
      exports: ["useOnboarding"]
    - path: "front/features/onboarding/api/onboarding-api.ts"
      provides: "API wrappers: getOnboardingStatus, saveScenario, saveLocation, saveBudget, saveDealbreakers"
      exports: ["getOnboardingStatus", "saveScenario", "saveLocation", "saveBudget", "saveDealbreakers"]
    - path: "front/shared/lib/api/geo.ts"
      provides: "getCities, getDistricts"
      exports: ["getCities", "getDistricts", "City", "District"]
    - path: "front/widgets/home/ui/HomeView.tsx"
      provides: "Onboarding gate: renders OnboardingFlow or SwipeDeck based on onboardingCompleted"
  key_links:
    - from: "HomeView"
      to: "OnboardingFlow"
      via: "useState(onboardingCompleted) checked after auth.status === 'authenticated'"
      pattern: "onboardingCompleted ? SwipeDeck : OnboardingFlow"
    - from: "useOnboarding mount effect"
      to: "GET /onboarding/status"
      via: "getOnboardingStatus() called once on mount with cancellation guard"
      pattern: "if (status.onboardingCompleted) dispatch COMPLETE; else if (onboardingStep > 0) dispatch SET_STEP + UPDATE_ANSWERS"
    - from: "useOnboarding BackButton effect"
      to: "Telegram.WebApp.BackButton"
      via: "useEffect watching state.step, handleBack as useCallback"
      pattern: "step > 0 && step < 6: show() + onClick(handleBack); else: hide(). Cleanup: offClick(handleBack)"
---

<objective>
Build the frontend onboarding slice — steps 0 through 3 (Scenario, Location, Budget, Dealbreakers). This includes the state machine hook, API client wrappers, all step components, and the HomeView gate that switches between onboarding and SwipeDeck.

Purpose: Steps 0–3 are the first half of the onboarding flow (the data-gathering steps before quiz). They share the same `useOnboarding` hook and `OnboardingFlow` container that steps 4–6 will also use. Getting this foundation right means Plan 04 only needs to add step components.

Output: 12 files. Onboarding flow is navigable from step 0 to 3, with each step persisting to the backend. HomeView gate is live. BackButton wired.
</objective>

<execution_context>
@E:/Project/git project/roomies/.planning/phases/1/01-RESEARCH.md
@E:/Project/git project/roomies/.planning/phases/1/01-PATTERNS.md
@E:/Project/git project/roomies/.planning/phases/1/01-UI-SPEC.md
</execution_context>

<context>
@E:/Project/git project/roomies/.planning/phases/1-CONTEXT.md
@E:/Project/git project/roomies/front/widgets/home/ui/HomeView.tsx
@E:/Project/git project/roomies/front/features/auth/model/use-telegram-auth.ts
@E:/Project/git project/roomies/front/shared/lib/api/client.ts
@E:/Project/git project/roomies/front/shared/lib/api/auth.ts

<interfaces>
<!-- apiFetch signature (from shared/lib/api/client.ts) -->
export async function apiFetch<T>(
  path: string,
  { body, auth = true, headers, ...init }: ApiFetchOptions = {},
): Promise<T>
-- auth defaults to true (sends Bearer token from localStorage)
-- body is serialized to JSON automatically
-- throws ApiError on non-2xx response

<!-- ApiError class (from shared/lib/api/client.ts) -->
export class ApiError extends Error {
  constructor(public readonly status: number, message: string, public readonly body?: unknown)
}

<!-- useTelegramAuth return type (from use-telegram-auth.ts) -->
type AuthState = { status: 'idle'|'authenticating'|'authenticated'|'error'; tokens: AuthTokens|null; error: string|null }

<!-- getWebApp() — from @/shared/lib/telegram -->
-- Returns Telegram.WebApp or null if outside Telegram
-- Used for BackButton.show(), BackButton.hide(), BackButton.onClick(fn), BackButton.offClick(fn)
-- Also used for initDataUnsafe.user.first_name (ProfileStep auto-fill in Plan 04)

<!-- HomeView.tsx current content -->
-- Line 3: import { MOCK_PROFILES } from '@/entities/profile';
-- Line 4: import { AuthStatusChip, useTelegramAuth } from '@/features/auth';
-- Line 5: import { useTelegramWebApp } from '@/shared/lib/telegram';
-- Line 6: import { SwipeDeck } from '@/widgets/swipe-deck';
-- Line 37: <SwipeDeck profiles={MOCK_PROFILES} />
-- Modifications: add useState, import OnboardingFlow, add onboarding gate

<!-- FSD layer rules (must not violate) -->
-- HomeView (views/) may import from features/ ✓
-- OnboardingFlow (features/) may NOT import from views/ or widgets/ ✗
-- shared/lib/api/ files import from './client' (NOT '@/shared/lib/api' — would be circular)
-- All .tsx ui/ files and use-*.ts model/ files: 'use client' as first line
-- types.ts and quiz-questions.ts: no 'use client' needed

<!-- Tailwind v4 tokens (from globals.css via UI-SPEC) -->
bg-surface → var(--surface) → #ffffff
bg-background → var(--bg) → #f6f6f1
bg-lavender → var(--lavender) → #d6cffa
bg-accent → var(--accent) → #c8f36a
text-(--text) → #14140f
text-(--text-muted) → #6f6f68
text-(--text-on-accent) → #14140f
style={{ boxShadow: 'var(--shadow-button)' }} for card/button shadows
h-dvh (not h-screen — Telegram WebView)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Types, API client, useOnboarding hook, geo API</name>
  <read_first>
    E:/Project/git project/roomies/front/features/auth/model/use-telegram-auth.ts
    E:/Project/git project/roomies/front/shared/lib/api/auth.ts
    E:/Project/git project/roomies/front/shared/lib/api/client.ts
    E:/Project/git project/roomies/.planning/phases/1/01-PATTERNS.md
    (sections: types.ts pattern, onboarding-api.ts pattern, use-onboarding.ts pattern, geo.ts pattern)
  </read_first>
  <behavior>
    - `types.ts` exports: ScenarioType, GuestsPreference, QuizAnswer, OnboardingAnswers, OnboardingState, OnboardingAction, OnboardingStatus, LocationPayload, BudgetPayload, DealbreakersPayload, QuizPayload, ProfilePayload
    - `getOnboardingStatus()` calls GET /onboarding/status with auth:true → returns OnboardingStatus
    - `saveScenario("looking_housing_roomie")` calls PATCH /onboarding/scenario with body { scenario } → returns { ok: boolean }
    - `saveLocation({ cityId: 1, districtIds: [1,2] })` calls PATCH /onboarding/location
    - `saveBudget({ budgetMin: 20000, budgetMax: 50000 })` calls PATCH /onboarding/budget
    - `saveDealbreakers({ smokingOk: false, petsOk: false, guestsPref: "sometimes" })` calls PATCH /onboarding/dealbreakers
    - `getCities()` calls GET /geo/cities with auth:false → returns City[]
    - `getDistricts(1)` calls GET /geo/cities/1/districts with auth:false → returns District[]
    - `useOnboarding()` returns `{ state: OnboardingState, submitScenario, submitLocation, submitBudget, submitDealbreakers, onComplete }`
    - On mount, useOnboarding calls getOnboardingStatus. If `status.onboardingCompleted === true`, dispatches COMPLETE (sets state.onboardingCompleted=true). Else if onboardingStep > 0, dispatches SET_STEP + UPDATE_ANSWERS. This ensures returning users who already completed onboarding are detected immediately so OnboardingFlow can signal HomeView to skip to SwipeDeck.
    - If getOnboardingStatus fails, error is silently ignored and flow starts fresh at step 0
    - When step === 0: BackButton hidden. When step in 1..5: BackButton shown, onClick decrements step. Cleanup: offClick handler.
    - When step === 6: BackButton hidden (DoneStep — no going back)
    - submitScenario dispatches SET_LOADING:true, calls saveScenario, dispatches UPDATE_ANSWERS + SET_STEP:1 on success, SET_ERROR on failure, SET_LOADING:false in finally
    - Same pattern for submitLocation, submitBudget, submitDealbreakers
    - Error message extracted: `e instanceof ApiError ? e.message : e instanceof Error ? e.message : 'Что-то пошло не так'`
  </behavior>
  <action>
    FILE: `front/features/onboarding/model/types.ts`
    - Pure type file, no `'use client'`. Export all types verbatim from PATTERNS.md "types.ts pattern" section. No runtime code.

    FILE: `front/features/onboarding/api/onboarding-api.ts`
    - First line: `'use client';`. Import `apiFetch` from `@/shared/lib/api`. Import types from `'../model/types'`.
    - Export functions: `getOnboardingStatus`, `saveScenario`, `saveLocation`, `saveBudget`, `saveDealbreakers`, `saveQuiz`, `saveProfile`.
    - All onboarding API calls use `auth: true` (default). HTTP methods per CONTEXT.md Decision 6 table.
    - `saveQuiz` uses method: 'POST', all others use method: 'PATCH'.
    - `getOnboardingStatus` uses method: 'GET'.

    FILE: `front/shared/lib/api/geo.ts`
    - First line: `'use client';`. Import `apiFetch` from `'./client'` (NOT `@/shared/lib/api` — circular).
    - Export: `type City = { id: number; name: string; countryCode: string }`, `type District = { id: number; cityId: number; name: string }`.
    - `getCities()`: `apiFetch<City[]>('/geo/cities', { method: 'GET', auth: false })`.
    - `getDistricts(cityId: number)`: `apiFetch<District[]>(\`/geo/cities/\${cityId}/districts\`, { method: 'GET', auth: false })`.

    UPDATE: `front/shared/lib/api/index.ts`
    - Append (do not remove existing exports): `export { getCities, getDistricts, type City, type District } from './geo';`
    - Do NOT add the vibe-tags export here — `vibe-tags.ts` does not exist until Plan 04. The vibe-tags export will be added in Plan 04 Task 1.

    FILE: `front/features/onboarding/model/use-onboarding.ts`
    - First line: `'use client';`. Follow PATTERNS.md "use-onboarding.ts pattern" section exactly.
    - Imports: `useEffect, useReducer, useCallback` from `'react'`; `getWebApp` from `'@/shared/lib/telegram'`; API functions from `'../api/onboarding-api'`; types from `'./types'`; `ApiError` from `'@/shared/lib/api'`.
    - `initialAnswers`: all nullable/empty per PATTERNS.md.
    - `reducer`: pure function outside hook — handles SET_STEP, SET_LOADING, SET_ERROR, UPDATE_ANSWERS, COMPLETE. COMPLETE sets `state.onboardingCompleted = true` and `state.step = 6`. `OnboardingState` includes `onboardingCompleted: boolean` (default false).
    - `useOnboarding()` hook:
      - `useReducer(reducer, initialState)`.
      - Mount effect: `getOnboardingStatus().then(status => { if (cancelled) return; if (status.onboardingCompleted) { dispatch({ type: 'COMPLETE' }); } else if (status.onboardingStep > 0) { dispatch SET_STEP + UPDATE_ANSWERS(hydrate all fields); } }).catch(() => {})`. COMPLETE dispatch sets state.onboardingCompleted=true which OnboardingFlow watches to immediately call its outer onComplete prop — handles returning users who already finished onboarding.
      - Cancellation: `let cancelled = false; return () => { cancelled = true }`.
      - BackButton effect: `useEffect(() => { const wa = getWebApp(); if (!wa) return; if (state.step > 0 && state.step < 6) { wa.BackButton.show(); wa.BackButton.onClick(handleBack); } else { wa.BackButton.hide(); } return () => { wa.BackButton.offClick(handleBack); }; }, [state.step, handleBack])`.
      - `handleBack`: `useCallback(() => dispatch({ type: 'SET_STEP', step: state.step - 1 }), [state.step])`.
      - Submit handlers (submitScenario, submitLocation, submitBudget, submitDealbreakers): each dispatches SET_LOADING:true, calls API, dispatches UPDATE_ANSWERS + SET_STEP on success, SET_ERROR on failure, SET_LOADING:false in finally. Step numbers: scenario→1, location→2, budget→3, dealbreakers→4.
      - `onComplete`: `() => dispatch({ type: 'COMPLETE' })` — used by DoneStep in Plan 04.
      - Return `{ state, submitScenario, submitLocation, submitBudget, submitDealbreakers, onComplete }`. Plan 04 will add submitQuiz, submitProfile.
  </action>
  <verify>
    <automated>cd front && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes with 0 errors on files modified in this task
    - types.ts has no `'use client'` directive (pure types)
    - onboarding-api.ts first line is `'use client';`
    - use-onboarding.ts BackButton cleanup returns `offClick(handleBack)` in every useEffect branch
    - geo.ts imports from `'./client'` not `'@/shared/lib/api'`
    - useOnboarding mount effect has `let cancelled = false` guard
  </acceptance_criteria>
  <done>
    types.ts, use-onboarding.ts (steps 0-3 submit handlers), onboarding-api.ts, geo.ts committed. TypeScript compiles without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Step components 0–3, OnboardingFlow, HomeView gate</name>
  <read_first>
    E:/Project/git project/roomies/front/widgets/home/ui/HomeView.tsx
    E:/Project/git project/roomies/front/features/auth/model/use-telegram-auth.ts
    E:/Project/git project/roomies/.planning/phases/1/01-UI-SPEC.md
    E:/Project/git project/roomies/.planning/phases/1/01-PATTERNS.md
    (sections: ScenarioStep.tsx, LocationStep.tsx, OnboardingFlow.tsx pattern, HomeView modifications)
  </read_first>
  <action>
    Create reusable components and step components. All .tsx files start with `'use client';`.

    FILE: `front/features/onboarding/ui/OnboardingFlow.tsx`
    - Props: `{ onComplete: () => void }`.
    - Import `useOnboarding` from `'../model/use-onboarding'`.
    - Import `useEffect, useCallback` from `'react'`.
    - Import step components (ScenarioStep, LocationStep, BudgetStep, DealbreakersStep).
    - After destructuring `useOnboarding()`, add: `useEffect(() => { if (state.onboardingCompleted) { onComplete(); } }, [state.onboardingCompleted, onComplete]);` — this fires immediately for returning users whose mount effect dispatched COMPLETE, so HomeView skips OnboardingFlow and shows SwipeDeck without the user having to tap anything.
    - Add placeholder imports for QuizStep, ProfileStep, DoneStep — these will be created in Plan 04. Use a simple `<div>TODO Step {step}</div>` fallback for steps 4, 5, 6 until Plan 04 creates them.
    - Root element: `<main className="mx-auto flex h-dvh w-full max-w-md flex-col">`.
    - Render error toast if `state.error`: `<div role="alert" className="fixed top-4 left-4 right-4 z-50 rounded-2xl bg-surface px-4 py-3 text-sm text-(--text)" style={{ boxShadow: 'var(--shadow-card)' }}>` with error text and dismiss button.
    - Render the correct step based on `state.step` (0→ScenarioStep, 1→LocationStep, 2→BudgetStep, 3→DealbreakersStep, 4–6→TODO div).
    - Pass `state` and appropriate submit handler from `useOnboarding` to each step.
    - Wrap step render in a keyed `<div key={state.step} className="flex flex-1 flex-col overflow-y-auto">` for slide transition — CSS class `translate-x-0 transition-transform duration-[280ms] ease-out`.
    - When step advances, OnboardingFlow calls the passed `onComplete` callback from `useOnboarding`'s `onComplete` — wire in DoneStep (Plan 04).

    FILE: `front/features/onboarding/ui/steps/ScenarioStep.tsx`
    Per UI-SPEC Screen 0. Layout with:
    - `ProgressBar` (current=0, total=6) at top.
    - Heading: `<h1 className="text-2xl font-bold text-(--text)">Ты ищешь или уже нашёл жильё?</h1>`.
    - Hint: `<p className="text-base text-(--text-muted) mt-2">Выбери свой сценарий</p>`.
    - 4 scenario cards (`role="radio"`, `aria-checked={selected}`). Card spec from UI-SPEC: `rounded-2xl bg-surface px-4 py-4 flex items-center gap-3` + `style={{ boxShadow: 'var(--shadow-button)' }}`. Selected: `ring-2 ring-accent`. One selection only (radio behavior via local `useState`).
    - Cards: `{ scenario: 'looking_housing_roomie', emoji: '🏠', title: 'Ищу жильё и соседа', subtitle: 'Нет квартиры, нужен кто-то рядом' }`, `{ scenario: 'has_housing_seeking_roomie', emoji: '🔑', title: 'У меня есть жильё', subtitle: 'Ищу соседа на свою площадь' }`, `{ scenario: 'looking_roomie_find_housing', emoji: '🤝', title: 'Ищем вместе', subtitle: 'Уже есть сосед, ищем квартиру' }`, `{ scenario: 'squad', emoji: '👥', title: 'Собираем команду', subtitle: 'Сквад на 2–4 человека' }`.
    - PrimaryButton: label "Продолжить", `disabled={!selected}`, `loading={state.loading}`, `onClick={() => onSubmit(selected!)}`. Button: `h-14 w-full rounded-2xl bg-accent text-(--text-on-accent) font-semibold text-base disabled:opacity-50 active:scale-[0.97] transition-transform`.
    - On tap of scenario card: `haptic('light')` from `@/shared/lib/telegram`.
    - Props: `{ state: OnboardingState; onSubmit: (scenario: ScenarioType) => void }`.

    FILE: `front/features/onboarding/ui/steps/LocationStep.tsx`
    Per UI-SPEC Screen 1. Uses `getCities`, `getDistricts` from `@/shared/lib/api`.
    - Local state: `cities: City[]`, `districts: District[]`, `loadingCities: boolean`, `loadingDistricts: boolean`, `selectedCityId: number|null` (pre-filled from `state.answers.cityId`), `selectedDistrictIds: number[]` (pre-filled from `state.answers.districtIds`).
    - On mount: `getCities()` with cancellation guard (let cancelled pattern). If error, show inline retry.
    - On city change: `getDistricts(cityId)` call.
    - City select: styled `<select className="w-full rounded-2xl bg-surface h-14 px-4 text-base text-(--text) appearance-none">` with custom chevron (inline SVG overlay). Placeholder option "Выбери город" with value="".
    - District chips: appear when `districts.length > 0`. Each chip: `role="checkbox"`, `aria-checked={selected}`. Selected: `bg-lavender`. Multi-select.
    - PrimaryButton: "Продолжить", `disabled={!selectedCityId}`, calls `onSubmit({ cityId: selectedCityId!, districtIds: selectedDistrictIds })`.
    - Props: `{ state: OnboardingState; onSubmit: (payload: LocationPayload) => void }`.
    - Heading: "Где ищешь?" (text-2xl font-bold), Hint: "Город — обязательно, районы — по желанию" (text-base text-muted mt-2).

    FILE: `front/features/onboarding/ui/steps/BudgetStep.tsx`
    Per UI-SPEC Screen 2.
    - Local state: `budgetMin`, `budgetMax`, `moveInDate`, `stayDurationMonths` (all pre-filled from state.answers).
    - Two number inputs side by side: `<input type="number" className="flex-1 rounded-2xl bg-surface h-14 px-4 text-base text-(--text) text-center"`. Labels "от" / "до" + "₽" suffix.
    - Inline validation error if `budgetMin > 0 && budgetMax > 0 && budgetMin > budgetMax`: show `<p role="alert" className="text-sm mt-1" style={{ color: 'var(--rose)' }}>Минимальный бюджет не может быть больше максимального</p>`.
    - Date input: `<input type="date">`, full width. Stay duration: `<select>` with options: "" (empty/Не знаю), "1", "2", "3", "6", "12".
    - PrimaryButton: "Продолжить", always enabled (validation is informational), calls `onSubmit({ budgetMin: budgetMin||0, budgetMax: budgetMax||0, moveInDate: moveInDate||undefined, stayDurationMonths: stayDurationMonths?Number(stayDurationMonths):undefined })`.
    - Heading: "Какой у тебя бюджет?" Secondary heading: "Когда планируешь?" (text-base font-semibold).
    - Props: `{ state: OnboardingState; onSubmit: (payload: BudgetPayload) => void }`.

    FILE: `front/features/onboarding/ui/steps/DealbreakersStep.tsx`
    Per UI-SPEC Screen 3.
    - Local state: `smokingOk`, `petsOk`, `guestsOn: boolean`, `guestsPref: GuestsPreference` (all pre-filled from state.answers). `guestsOn` = `state.answers.guestsPref !== 'rarely'` initially.
    - 3 toggle rows. Toggle switch: `<button role="switch" aria-checked={checked} onClick={...} className="relative w-12 h-7 rounded-full transition-colors duration-200 ..." style={{ background: checked ? 'var(--accent)' : 'var(--surface-2)' }}>`. White circle thumb inside.
    - Guests sub-selector (3 chips: "Редко"/"rarely", "Иногда"/"sometimes", "Часто"/"often"): appears in `<div style={{ maxHeight: guestsOn ? '60px' : '0', overflow: 'hidden', transition: 'max-height 200ms ease-in-out' }}>`. Selected chip: `bg-accent text-(--text-on-accent)`.
    - When guests toggle turned ON: set guestsPref to 'sometimes'. When turned OFF: guestsPref effectively becomes 'rarely'.
    - PrimaryButton: "Продолжить", always enabled. `onSubmit({ smokingOk, petsOk, guestsPref: guestsOn ? guestsPref : 'rarely' })`.
    - Heading: "Что для тебя важно?" Hint: "Это влияет на совместимость".
    - Toggle rows: `{ emoji: '🚬', label: 'Курение окей', checked: smokingOk, onChange: () => setSmokingOk(!smokingOk) }`, `{ emoji: '🐾', label: 'Питомцы окей', checked: petsOk, ... }`, `{ emoji: '🎉', label: 'Гости бывают', checked: guestsOn, ... }`.
    - Props: `{ state: OnboardingState; onSubmit: (payload: DealbreakersPayload) => void }`.

    FILE: `front/features/onboarding/index.ts`
    - No `'use client'`. Export: `export { OnboardingFlow } from './ui/OnboardingFlow'`; `export { useOnboarding } from './model/use-onboarding'`; `export type { OnboardingState, OnboardingAnswers } from './model/types'`.

    UPDATE: `front/widgets/home/ui/HomeView.tsx`
    - Add `import { useState } from 'react';` (after existing imports).
    - Add `import { OnboardingFlow } from '@/features/onboarding';`.
    - Inside `HomeView()` after `const auth = useTelegramAuth();`: add `const [onboardingCompleted, setOnboardingCompleted] = useState(false);`.
    - Replace `<SwipeDeck profiles={MOCK_PROFILES} />` with:
      `{onboardingCompleted ? <SwipeDeck profiles={MOCK_PROFILES} /> : <OnboardingFlow onComplete={() => setOnboardingCompleted(true)} />}`.
    - Remove the wrapping `<main>` padding/gap since `OnboardingFlow` provides its own layout. The conditional render replaces the entire inner content area. Keep `auth.status === 'error'` block above the conditional — it's still useful.
    - NOTE: Keep header section and error block. Only replace the SwipeDeck line. The outer `<main>` still has its existing className. OnboardingFlow renders its own `<main>` inside — this creates a nested main. Adjust: wrap OnboardingFlow and SwipeDeck in a `<div className="flex flex-1 flex-col">` instead of directly inside outer main. The outer main keeps header + error; the inner area is the flow content.
    - Actually per PATTERNS.md "HomeView modifications": the onboarding gate replaces `<SwipeDeck>` directly. OnboardingFlow has its own full-page layout. So HomeView should: when `!onboardingCompleted`, render only `<OnboardingFlow onComplete={...} />` WITHOUT the header/footer, so the full screen belongs to onboarding. When `onboardingCompleted`, render the existing header + SwipeDeck.
    - Implementation: check if `auth.status === 'authenticated'` AND `!onboardingCompleted` → return `<OnboardingFlow onComplete={() => setOnboardingCompleted(true)} />` early (before the main return). This avoids restructuring the main return.
  </action>
  <verify>
    <automated>cd front && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` in `front/` exits 0
    - `npm run dev` in `front/` starts without console errors
    - In browser (or Telegram): HomeView shows `<OnboardingFlow>` when onboardingCompleted=false
    - ScenarioStep renders 4 cards; selecting one enables "Продолжить" button
    - LocationStep renders city dropdown; selecting a city triggers district load
    - BudgetStep renders two numeric inputs side by side
    - DealbreakersStep renders 3 toggle rows; guests toggle reveals sub-selector
    - All step cards use `bg-surface` background with `var(--shadow-button)` shadow
    - Telegram BackButton appears on steps 1–3, hidden on step 0
    - No `h-screen` in any new component — only `h-dvh`
    - `OnboardingFlow` root element has `className` containing `h-dvh`
    - ScenarioStep cards have `role="radio"` and `aria-checked` attributes
    - DealbreakersStep toggles have `role="switch"` and `aria-checked`
  </acceptance_criteria>
  <done>
    Steps 0–3 fully rendered and navigable. HomeView gate switches between OnboardingFlow and SwipeDeck. TypeScript compiles. BackButton wired.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram WebView → frontend | initData arrives from Telegram SDK; auth handled by existing AuthModule |
| frontend → backend API | All onboarding PATCH calls require JWT; API client adds Bearer token from localStorage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Frontend submits arbitrary scenario value | accept | Backend ScenarioDto validates via @IsEnum — invalid values rejected with 400. Frontend also enforces selection from predefined list. |
| T-03-02 | Information Disclosure | GET /onboarding/status returns step + saved fields on every OnboardingFlow mount | accept | Data belongs to the authenticated user. No cross-user exposure. Token required. |
| T-03-03 | Denial of Service | LocationStep triggers getCities + getDistricts API calls on every city select | accept | Two small DB reads. Cancellation pattern prevents stale state. No debounce needed at this scale. |
| T-03-04 | Spoofing | localStorage JWT manipulated by malicious script | accept | Telegram Mini App runs in sandboxed WebView. Backend validates JWT signature independently. |
</threat_model>

<verification>
## Plan 03 Verification

```bash
# 1. TypeScript compiles
cd front && npx tsc --noEmit
# Exit 0, 0 errors

# 2. Dev server starts
cd front && npm run dev
# "Ready" in output, no compile errors

# 3. Manual browser check (localhost:3001 or whatever port)
# - Open / (HomeView)
# - If auth.status==='authenticated' and onboardingCompleted===false: OnboardingFlow renders
# - Step 0: 4 scenario cards visible
# - Select a card: CTA enables
# - (With backend running) tap CTA: PATCH /onboarding/scenario called, step advances to 1
# - Step 1: city dropdown visible
# - BackButton visible (check Telegram test mode)

# 4. API layer check (with backend running + valid JWT)
curl -s http://localhost:3000/geo/cities | node -e "const d=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8')); process.stdout.write(d.length+' cities\n')"
# "7 cities"
```
</verification>

<success_criteria>
- `npx tsc --noEmit` exits 0 in `front/`
- HomeView renders `<OnboardingFlow>` when `onboardingCompleted===false` and auth is complete
- Steps 0–3 render with correct UI per UI-SPEC (headings, chips, toggles)
- ScenarioStep CTA disabled until card selected
- LocationStep loads cities from `/geo/cities`, districts from `/geo/cities/:id/districts`
- DealbreakersStep guests sub-selector shows/hides with max-height transition
- BackButton shows on steps 1–3, hides on steps 0 and 6
- BackButton cleanup: `offClick(handleBack)` in useEffect cleanup
- No `h-screen` — all layouts use `h-dvh`
</success_criteria>

<output>
After completion, create `.planning/phases/1/01-03-SUMMARY.md` documenting:
- Files created and their exports
- Any deviations from UI-SPEC (e.g. simplified toggle implementation)
- Confirmation that BackButton cleanup pattern is correct (offClick in cleanup)
- Any TypeScript strictness workarounds needed
</output>
