---
phase: 03-discovery-profiles
plan: 05
type: execute
wave: 3
depends_on:
  - "03-03"
files_modified:
  - "front/widgets/candidate-profile/ui/CandidateProfileSheet.tsx"
  - "front/widgets/candidate-profile/index.ts"
autonomous: true
requirements:
  - MATCH-07
  - PROF-02
  - PROF-03

must_haves:
  truths:
    - "CandidateProfileSheet slides up from bottom with spring animation when open=true"
    - "Sheet shows 4 sections in order: ВАЙБ ДОМА (VibeScaleBars), ПРАВИЛА (RulesSection), О СЕБЕ (budget/date/districts/tags), ПОЧЕМУ ВЫ СОВПАЛИ (MatchReasonsList)"
    - "Sheet sticky bottom has Pass and Like buttons (ActionButtons) with onSave and onSuperLike"
    - "Drag-to-dismiss: dragging handle area down > 100px closes sheet"
    - "Sheet can be dismissed by tapping backdrop"
    - "Like and Pass buttons call useSwipeMutation and close the sheet"
    - "Save and SuperLike buttons call useSwipeMutation with respective actions and close the sheet"
  artifacts:
    - path: "front/widgets/candidate-profile/ui/CandidateProfileSheet.tsx"
      provides: "CandidateProfileSheet with all 4 sections and swipe action buttons"
    - path: "front/widgets/candidate-profile/index.ts"
      provides: "barrel exporting CandidateProfileSheet"
  key_links:
    - from: "front/widgets/candidate-profile/ui/CandidateProfileSheet.tsx"
      to: "front/shared/ui/vibe-scale-bar/VibeScaleBar.tsx"
      via: "import { VibeScaleBar } from '@/shared/ui/vibe-scale-bar'"
      pattern: "VibeScaleBar"
    - from: "front/widgets/candidate-profile/ui/CandidateProfileSheet.tsx"
      to: "front/features/swipe-profile/index.ts"
      via: "import { ActionButtons, useSwipeMutation } from '@/features/swipe-profile'"
      pattern: "ActionButtons|useSwipeMutation"
---

<objective>
Build the CandidateProfileSheet widget that opens when a user taps a swipe card. Contains the full candidate profile with 4 sections plus Like/Pass/Save/SuperLike action buttons, following FilterSheet CSS pattern exactly.

Purpose: Delivers PROF-02 (detailed profile sections), PROF-03 (matchReasons block), and MATCH-07 (4 swipe actions from the sheet). This is the core discovery UX for Phase 3.
Output: CandidateProfileSheet widget with barrel export.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-discovery-profiles/03-CONTEXT.md
@.planning/phases/03-discovery-profiles/03-UI-SPEC.md
@.planning/phases/03-discovery-profiles/03-03-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs for this component. -->

CandidateProfileSheet props:
```typescript
interface CandidateProfileSheetProps {
  profile: RoomieProfile;   // from @/entities/profile — has matchReasons, matchRisks, smokingOk, petsOk, guestsPref, lifestyleScales
  open: boolean;
  onClose: () => void;
  onSwipe: (action: 'like' | 'pass' | 'save' | 'super_like') => void;
}
```

From front/widgets/swipe-deck/ui/FilterSheet.tsx — sheet container classes (MUST copy exactly):
```
fixed inset-x-0 bottom-0 z-50
flex flex-col
max-h-[92dvh]
rounded-t-3xl border-t-2 border-black bg-white
shadow-[0_-4px_0_rgba(20,20,15,0.9)]
transition-transform duration-300 ease-[cubic-bezier(0.34,1.2,0.64,1)]
open: translate-y-0   closed: translate-y-full
```

Backdrop (lower z than FilterSheet — z-40):
```
fixed inset-0 z-40 bg-black/40 backdrop-blur-sm
transition-opacity duration-300
open: opacity-100  closed: pointer-events-none opacity-0
```

Sheet layout structure (from 03-UI-SPEC.md):
1. Drag handle (shrink-0): div mx-auto h-1 w-10 rounded-full bg-[#d0d0cc] pt-4
2. Header (shrink-0, px-5 pt-2 pb-4): photo 80x80 + name,age + match % badge
3. Scrollable content (flex-1 overflow-y-auto overscroll-contain px-5): 4 sections
4. Sticky footer (shrink-0, px-5 pb-[max(2rem,env(safe-area-inset-bottom))]): ActionButtons

Section header pattern (each): span className="text-xs font-black uppercase tracking-widest text-[#6f6f68]"

Header match badge: span className="rounded-md border-2 border-black bg-[#c8f36a] px-2 py-1 text-sm font-black -rotate-2 shadow-[2px_2px_0_rgba(20,20,15,0.9)]" — text: `★ {matchPct}%`

Photo: img/next/Image h-20 w-20 rounded-2xl border-2 border-black shadow-[3px_3px_0_rgba(20,20,15,0.9)] object-cover. If no photo: div with house emoji.

VibeScaleBar SCALE_KEYS order: ['noiseLevel', 'cleanliness', 'sleepSchedule', 'socialLevel', 'workFromHome']

О СЕБЕ section content:
- Budget: text-sm — 'Бюджет: X – Y тыс.' or 'Бюджет не указан'
- Scenario label map: looking_housing_roomie → 'Ищу жильё и соседа', has_housing_seeking_roomie → 'Есть жильё, ищу соседа', looking_roomie_find_housing → 'Ищу соседей', squad → 'Сквад'
- Districts: comma-joined district names
- Vibe tags: flex-wrap chips with TAG_COLORS pattern from ProfileCard (bg-[#c8f36a], bg-[#a8d8ff], bg-[#ffb8d4])

Drag-to-dismiss: track touchStart Y in a ref; on touchEnd if deltaY > 100 → call onClose(). Use onPointerDown+onPointerUp on the handle area only (top 80px), or on the scroll container if at top scroll position. Pattern: onPointerDown on handle → record startY. onPointerMove → compute dy. onPointerUp → if dy > 100 → onClose().

useSwipeMutation: import from '@/features/swipe-profile'. Call .mutateAsync({targetId: profile.id, action}). Use haptic('medium') before call. onSwipe callback tells parent (SwipeDeck) to update deck state.

Escape key: useEffect on window keydown → if 'Escape' → onClose().

aria attributes: role="dialog" aria-modal="true" aria-label="Профиль кандидата"

ActionButtons in sheet: pass onSave={() => { haptic('medium'); useSwipeMutation.mutateAsync({targetId:profile.id, action:'save'}); onSwipe('save'); onClose(); }} and similar for onSuperLike, onPass, onLike. The mutation is called inside the sheet; onSwipe informs the parent to advance the deck.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build CandidateProfileSheet</name>
  <files>
    front/widgets/candidate-profile/ui/CandidateProfileSheet.tsx
    front/widgets/candidate-profile/index.ts
  </files>
  <read_first>
    - front/widgets/swipe-deck/ui/FilterSheet.tsx (full file — copy container, backdrop, handle, Escape handler pattern exactly)
    - front/features/swipe-profile/ui/ActionButtons.tsx (full file — import and reuse; new props onSave/onSuperLike from plan 03-04)
    - front/shared/ui/vibe-scale-bar/index.ts (confirm VibeScaleBar export path)
    - front/shared/ui/rules-section/index.ts (confirm RulesSection export path)
    - front/shared/ui/match-reasons-list/index.ts (confirm MatchReasonsList export path)
    - front/entities/profile/model/types.ts (RoomieProfile type with new fields from plan 03-03)
    - front/features/swipe-profile/index.ts (confirm useSwipeMutation export)
    - front/shared/lib/telegram/index.ts (confirm haptic export)
  </read_first>
  <action>
    Create front/widgets/candidate-profile/ui/CandidateProfileSheet.tsx:

    'use client' directive.

    Imports:
    - useCallback, useEffect, useRef, useState from 'react'
    - Image from 'next/image'
    - RoomieProfile from '@/entities/profile'
    - ActionButtons, useSwipeMutation from '@/features/swipe-profile'
    - VibeScaleBar from '@/shared/ui/vibe-scale-bar'
    - RulesSection from '@/shared/ui/rules-section'
    - MatchReasonsList from '@/shared/ui/match-reasons-list'
    - haptic from '@/shared/lib/telegram'

    Props interface CandidateProfileSheetProps: { profile: RoomieProfile; open: boolean; onClose: () => void; onSwipe: (action: 'like' | 'pass' | 'save' | 'super_like') => void; }

    Constants inside component or module:
    - SCENARIO_LABELS: Record<string, string> = { looking_housing_roomie: 'Ищу жильё и соседа', has_housing_seeking_roomie: 'Есть жильё, ищу соседа', looking_roomie_find_housing: 'Ищу соседей', squad: 'Сквад' }
    - TAG_COLORS = ['bg-[#c8f36a]', 'bg-[#a8d8ff]', 'bg-[#ffb8d4]']
    - VIBE_KEYS: ('noiseLevel'|'cleanliness'|'sleepSchedule'|'socialLevel'|'workFromHome')[] = ['noiseLevel','cleanliness','sleepSchedule','socialLevel','workFromHome']

    Hooks:
    - const swipeMutation = useSwipeMutation()
    - Drag state: useRef for startY (pointer), ref for isDragging. onPointerDown on handle area: set startY. onPointerMove: track currentY. onPointerUp: if currentY - startY > 100 → onClose().

    Action handlers (useCallback):
    - handleAction(action: 'like'|'pass'|'save'|'super_like') → haptic('medium'); swipeMutation.mutate({targetId: profile.id, action}); onSwipe(action); onClose();
    - Wire: onLike → handleAction('like'), onPass → handleAction('pass'), onSave → handleAction('save'), onSuperLike → handleAction('super_like')

    Escape key: useEffect → window.addEventListener('keydown', handler) where handler calls onClose() on Escape.

    matchPct: Math.round(profile.matchScore * 100)

    JSX structure:
    1. Backdrop div: fixed inset-0 z-40 bg-black/40 backdrop-blur-sm transition-opacity duration-300; opacity-100 when open else pointer-events-none opacity-0; onClick={onClose}
    2. Sheet div: fixed inset-x-0 bottom-0 z-50 flex flex-col max-h-[92dvh] rounded-t-3xl border-t-2 border-black bg-white shadow-[0_-4px_0_rgba(20,20,15,0.9)] transition-transform duration-300 ease-[cubic-bezier(0.34,1.2,0.64,1)]; translate-y-0 when open else translate-y-full; role="dialog" aria-modal="true" aria-label="Профиль кандидата"
    3. Inside sheet, in order:
       a. Handle row (drag area, shrink-0, pt-4 pb-2, flex justify-center, cursor-grab): div mx-auto h-1 w-10 rounded-full bg-[#d0d0cc] — attach pointer handlers here
       b. Header (shrink-0, flex gap-4 items-start px-5 pt-2 pb-4):
          - Photo: profile.photos[0] ? <Image src={profile.photos[0]} ...> : house emoji div; className="h-20 w-20 rounded-2xl border-2 border-black shadow-[3px_3px_0_rgba(20,20,15,0.9)] object-cover"
          - Info div (flex flex-col gap-1):
            - Name+age: span className="text-2xl font-black text-[#14140f]" — "{profile.name}{profile.age ? ', ' + profile.age : ''}"
            - Match badge: span className="rounded-md border-2 border-black bg-[#c8f36a] px-2 py-1 text-sm font-black -rotate-2 shadow-[2px_2px_0_rgba(20,20,15,0.9)]" — `★ ${matchPct}%`
       c. Scrollable content (flex-1 overflow-y-auto overscroll-contain px-5 flex flex-col gap-5 pb-2):
          - Section "ВАЙБ ДОМА": header + div flex flex-col gap-2 with {VIBE_KEYS.map(k => profile.lifestyleScales?.[k] != null && <VibeScaleBar key={k} scaleKey={k} value={profile.lifestyleScales[k]} />)}
          - Section "ПРАВИЛА": header + <RulesSection smokingOk={profile.smokingOk} petsOk={profile.petsOk} guestsPref={profile.guestsPref} />
          - Section "О СЕБЕ": header + flex flex-col gap-1 with text-sm lines: budget string, scenario label, districts comma-joined, vibe tags chips (flex-wrap gap-1.5)
          - Section "ПОЧЕМУ ВЫ СОВПАЛИ": {profile.matchReasons.length > 0 && (header + <MatchReasonsList matchReasons={profile.matchReasons} matchRisks={profile.matchRisks} />)}
       d. Footer (shrink-0, px-5 pt-2 pb-[max(2rem,env(safe-area-inset-bottom))]): <ActionButtons onPass={() => handleAction('pass')} onLike={() => handleAction('like')} onSave={() => handleAction('save')} onSuperLike={() => handleAction('super_like')} />

    Section header pattern: span className="text-xs font-black uppercase tracking-widest text-[#6f6f68]"

    Budget string: profile.budgetMin && profile.budgetMax ? `${Math.round(profile.budgetMin/1000)}–${Math.round(profile.budgetMax/1000)} тыс./мес.` : profile.budgetMin ? `от ${Math.round(profile.budgetMin/1000)} тыс.` : 'Бюджет не указан'

    Create front/widgets/candidate-profile/index.ts:
    - export { CandidateProfileSheet } from './ui/CandidateProfileSheet'
  </action>
  <verify>
    <automated>cd front && npx tsc --noEmit 2>&1 | grep -i "candidate" | head -10</automated>
  </verify>
  <done>TypeScript clean for candidate-profile widget. CandidateProfileSheet renders 4 sections with correct component imports. ActionButtons receives all 4 action handlers as arrow function callbacks: onLike={() => handleAction('like')}, onPass={() => handleAction('pass')}, onSave={() => handleAction('save')}, onSuperLike={() => handleAction('super_like')} — NOT onLike={handleAction('like')} (which would call the function immediately on render). Sheet open/close animation classes applied correctly. index.ts barrel exports CandidateProfileSheet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CandidateProfileSheet → POST /swipes | Action button fires mutation with targetId from profile prop |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-11 | Tampering | CandidateProfileSheet onSwipe action | mitigate | TypeScript union type enforces 'like'|'pass'|'save'|'super_like'; backend validates via SwipeActionDto enum |
| T-03-12 | Information Disclosure | matchReasons displayed in sheet | accept | matchReasons are generated from the current user's own data vs candidate — no third-party data leaked |
| T-03-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
1. cd front && npx tsc --noEmit — no errors in candidate-profile widget
2. Visual: open sheet by tapping card → sheet slides up with spring animation (translate-y transition)
3. Visual: sheet shows photo, name, age, ★ N% badge
4. Visual: ВАЙБ ДОМА section shows VibeScaleBars for non-null scales
5. Visual: ПРАВИЛА shows 3 chips (smoking/pets/guests) with correct colors
6. Visual: О СЕБЕ shows budget, scenario, districts, vibe tags
7. Visual: ПОЧЕМУ ВЫ СОВПАЛИ shows matchReasons bullets; hidden if empty
8. Interaction: Like button fires POST /swipes with action='like' and closes sheet
9. Interaction: Save button fires POST /swipes with action='save'
10. Interaction: Drag handle down > 100px → sheet closes
11. Interaction: Backdrop tap → sheet closes
</verification>

<success_criteria>
- front/widgets/candidate-profile/ui/CandidateProfileSheet.tsx exists
- front/widgets/candidate-profile/index.ts exports CandidateProfileSheet
- Sheet animation: translate-y-0 when open, translate-y-full when closed
- All 4 sections rendered in correct order per D-04–D-08
- All 4 swipe actions wired to useSwipeMutation
- TypeScript build clean
</success_criteria>

<output>
Create .planning/phases/03-discovery-profiles/03-05-SUMMARY.md when done
</output>
