---
phase: 03-discovery-profiles
plan: 06
type: execute
wave: 4
depends_on:
  - "03-02"
  - "03-03"
files_modified:
  - "front/widgets/profile/ui/ProfileView.tsx"
  - "front/widgets/profile/ui/RoomieScoreCard.tsx"
  - "front/widgets/profile/ui/ProfileEditSheet.tsx"
autonomous: true
requirements:
  - PROF-01
  - PROF-04

must_haves:
  truths:
    - "ProfileView replaces the 4-line stub with a real own-profile screen"
    - "ProfileView loads own data via useProfileQuery() (GET /profile/me)"
    - "ProfileView displays: photo, name/age, vibe tags, RoomieScoreCard, budget/districts, VibeScaleBars (read-only), RulesSection (read-only)"
    - "RoomieScoreCard shows numeric score, progress bar, 4 tips with done/undone state"
    - "Кнопка 'Изменить' opens ProfileEditSheet"
    - "ProfileEditSheet lets user edit: photo URLs, name, vibe tags, budget, districts, dealbreakers"
    - "ProfileEditSheet 'Сохранить профиль' calls PATCH /profile via usePatchProfile and closes on success"
  artifacts:
    - path: "front/widgets/profile/ui/ProfileView.tsx"
      provides: "Own profile screen with useProfileQuery data"
    - path: "front/widgets/profile/ui/RoomieScoreCard.tsx"
      provides: "RoomieScoreCard component (score, progress bar, 4 tips)"
    - path: "front/widgets/profile/ui/ProfileEditSheet.tsx"
      provides: "ProfileEditSheet with form fields and PATCH /profile mutation"
  key_links:
    - from: "front/widgets/profile/ui/ProfileView.tsx"
      to: "front/features/profile/model/use-profile-query.ts"
      via: "useProfileQuery()"
      pattern: "useProfileQuery"
    - from: "front/widgets/profile/ui/ProfileEditSheet.tsx"
      to: "front/features/profile/model/use-profile-query.ts"
      via: "usePatchProfile()"
      pattern: "usePatchProfile"
    - from: "front/widgets/profile/ui/ProfileView.tsx"
      to: "front/shared/ui/vibe-scale-bar/index.ts"
      via: "VibeScaleBar (read-only)"
      pattern: "VibeScaleBar"
---

<objective>
Replace the ProfileView stub with a complete own-profile screen, build the RoomieScoreCard component (PROF-04), and build the ProfileEditSheet bottom sheet for profile editing (PROF-01).

Purpose: Closes the last two user-facing requirements for Phase 3 — users can see their Roomie Score and edit their profile. All data comes from the GET /profile/me endpoint created in plan 03-02.
Output: ProfileView (real), RoomieScoreCard, ProfileEditSheet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-discovery-profiles/03-CONTEXT.md
@.planning/phases/03-discovery-profiles/03-UI-SPEC.md
@.planning/phases/03-discovery-profiles/03-02-SUMMARY.md
@.planning/phases/03-discovery-profiles/03-03-SUMMARY.md

<interfaces>
<!-- Key contracts and visual spec. Extracted from 03-UI-SPEC.md and codebase. -->

MyProfile type (from plan 03-03 profile.ts):
```typescript
interface MyProfile {
  id: number; name: string; age?: number; scenario: string;
  budgetMin: number|null; budgetMax: number|null;
  smokingOk: boolean; petsOk: boolean; guestsPref: 'rarely'|'sometimes'|'often';
  photos: string[];
  vibeTags: { id: number; label: string }[];
  districts: { id: number; name: string }[];
  lifestyleScales: { noiseLevel: number|null; cleanliness: number|null;
    sleepSchedule: number|null; socialLevel: number|null; workFromHome: number|null; } | null;
  roomieScore: number;
  quizCompleted: boolean;
  onboardingStep: number;
}
```

useProfileQuery() → { data: MyProfile | undefined, isLoading, isError }
usePatchProfile() → useMutation from React Query; mutate({ name?, photoUrls?, ... })

ProfileView layout (from 03-UI-SPEC.md):
- Container: flex flex-col gap-6 overflow-y-auto px-5 py-4 pb-[max(2rem,env(safe-area-inset-bottom))]
- Header row: h3 'ПРОФИЛЬ' text-xs font-black uppercase tracking-widest + button 'Изменить' text-sm font-black underline underline-offset-2 text-[#14140f]
- Photo 96x96: rounded-2xl border-2 border-black shadow-[3px_3px_0_rgba(20,20,15,0.9)] object-cover; if no photo → div with '🏠' emoji
- Name + age: text-2xl font-black text-[#14140f]
- Vibe tags: flex flex-wrap gap-1.5 with TAG_COLORS chips
- RoomieScoreCard component
- Section "МОИ ПРЕДПОЧТЕНИЯ": budget string + districts comma-joined (text-sm text-[#14140f])
- Section "ВАЙБ ДОМА": 5 VibeScaleBars (read-only)
- Section "ПРАВИЛА": RulesSection (read-only)

RoomieScoreCard visual (from 03-UI-SPEC.md):
- Outer: border-2 border-black rounded-2xl bg-[#f0efe9] p-4 shadow-[4px_4px_0_rgba(20,20,15,0.9)]
- Header row: 'ROOMIE SCORE' (text-xs font-black uppercase tracking-widest text-[#6f6f68]) + score value '{score} / 40' (text-2xl font-black font-age text-[#14140f])
- Progress bar: h-3 w-full rounded-full border border-black bg-white overflow-hidden; fill div: h-full rounded-full bg-[#c8f36a] transition-all duration-700; width: Math.round(score/40*100)+'%'
- Tips list (flex flex-col gap-2 mt-3):
  Tips are fixed 4 items: photos (+10), quiz (+10), dealbreakers (+10), phone (+10, inactive)
  For each tip: done = score criteria met (photos.length>0, quizCompleted by checking score>=10 etc.) — BUT MyProfile doesn't include quizCompleted/onboardingStep. Simplify: compute which tips are done by the component knowing score. Actually score IS the source of truth. Instead derive done state from score value directly by each +10 increment.
  Better: Accept done states as props or compute heuristically. Since we have roomieScore (0/10/20/30/40), we can show progress but don't know which components. 
  Solution: backend PATCH /profile response includes fields to check. getMe() already returns the user. But MyProfile doesn't include quizCompleted/onboardingStep. Simplest MVP: Add quizCompleted and hasPhotos (or just compute from photos.length) to MyProfile. hasPhotos = profile.photos.length > 0. quizCompleted: add to MyProfile from GET /profile/me (backend already has it in select from onboarding.service). Update MyProfile type and backend getMe to include quizCompleted: boolean.
  For plan 03-06 scope: assume MyProfile already returns quizCompleted (backend plan 03-02 includes it). If not, assume score-based deduction: photos done if score indicates +10 for photos (use photos.length>0 from profile).
  Actually simplest: ProfileEditSheet will call getMe which returns photos[], so profile.photos.length > 0 = photo done. For quiz: add quizCompleted to MyProfile (executor should extend backend 03-02 getMe to include this field if not already present). dealbreakers done if profile.smokingOk is set (but it has a default, so check onboardingStep — or check guestsPref !== default). For MVP Phase 3: accept that tip done-states may be approximate. Use photos.length>0 for photo tip; quizCompleted if available; guestsPref !== 'sometimes' OR smokingOk for dealbreakers; isPhoneVerified always false.
  Tip text per UI spec: '📷 Добавить фото', '🎯 Vibe Quiz пройден', '📋 Правила указаны', '📞 Верифицировать телефон'
  Done tip: text-sm font-black text-[#14140f] line-through + chip bg-[#c8f36a]; Undone: text-sm font-black text-[#14140f] + chip bg-[#fff3a0]. Phone tip always undone (inactive), text-[#6f6f68].
  Chip style: px-2 py-0.5 text-xs font-black rounded-full border-2 border-black shadow-[2px_2px_0_rgba(20,20,15,0.9)]

ProfileEditSheet (same container pattern as FilterSheet = same as CandidateProfileSheet):
- Container: fixed inset-x-0 bottom-0 z-50 flex flex-col max-h-[92dvh] rounded-t-3xl border-t-2 border-black bg-white shadow-[0_-4px_0_rgba(20,20,15,0.9)] transition-transform duration-300 ease-[cubic-bezier(0.34,1.2,0.64,1)]; translate-y-0 when open
- Scrollable content area: flex-1 overflow-y-auto px-5 py-4 flex flex-col gap-5
- Sticky footer: 'Сохранить профиль' button: w-full rounded-full border-2 border-black bg-accent py-3 font-black text-[#14140f] shadow-[3px_3px_0_rgba(20,20,15,0.9)] active:translate-x-[1px] active:translate-y-[1px]
- Header: handle + 'Редактировать профиль' h3 + 'Отмена' button (onClose)

Photo section: horizontal scroll of thumbnails (h-[64px] w-[52px] rounded-xl border-2 border-black object-cover) + add button with URL input (text input placeholder='https://...' border-2 border-black rounded-xl px-4 py-3 text-sm) — MVP: URL-input. On 'Добавить' button click: if URL non-empty, append to local photoUrls array.

Name input: text input border-2 border-black rounded-xl px-4 py-3 text-sm font-black focus:ring-2 focus:ring-[#c8f36a] focus:outline-none

Vibe tags: fetch from GET /vibe-tags on mount; show as chips (max 3 selected). Selected chip: bg-[#c8f36a] border-2 border-black shadow-[2px_2px_0]; unselected: bg-white border-2 border-black. At limit (3 selected), unselected chips get opacity-50 pointer-events-none.

Budget: two text inputs ('от' and 'до') type="number" with same input style.

Districts: chips from GET /geo/districts — same fetch pattern as FilterSheet.

Dealbreakers: Row chips for smoking/pets/guests (same as FilterSheet filter rows).

Error state: if mutation fails, show text-xs text-[#ff6b85] below CTA button.

On successful save: close sheet (onClose()). haptic('success').

Vibe tags fetch: use local state + useEffect, apiFetch('/vibe-tags'). Returns { id, label }[].
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build RoomieScoreCard and ProfileView</name>
  <files>
    front/widgets/profile/ui/RoomieScoreCard.tsx
    front/widgets/profile/ui/ProfileView.tsx
  </files>
  <read_first>
    - front/widgets/profile/ui/ProfileView.tsx (current 4-line stub — will be fully replaced)
    - front/shared/ui/vibe-scale-bar/index.ts (confirm import path)
    - front/shared/ui/rules-section/index.ts (confirm import path)
    - front/features/profile/index.ts (confirm useProfileQuery import path)
    - front/app/globals.css (color tokens: --text, --text-muted, accent, font-age)
    - front/shared/lib/api/profile.ts (MyProfile type — check if quizCompleted is included)
  </read_first>
  <action>
    Create front/widgets/profile/ui/RoomieScoreCard.tsx:
    - 'use client'
    - Props: interface RoomieScoreCardProps { score: number; hasPhotos: boolean; quizCompleted?: boolean; dealbreakersSet?: boolean; }
    - MAX_SCORE = 40
    - pct = Math.round(score / MAX_SCORE * 100)
    - Tips array (4 fixed items): [{id:'photo', icon:'📷', text:'Добавить фото', points:10, done:props.hasPhotos}, {id:'quiz', icon:'🎯', text:'Vibe Quiz пройден', points:10, done:props.quizCompleted??false}, {id:'rules', icon:'📋', text:'Правила указаны', points:10, done:props.dealbreakersSet??false}, {id:'phone', icon:'📞', text:'Верифицировать телефон', points:10, done:false, inactive:true}]
    - Outer div: border-2 border-black rounded-2xl bg-[#f0efe9] p-4 shadow-[4px_4px_0_rgba(20,20,15,0.9)]
    - Header row (flex justify-between items-center mb-2): span 'ROOMIE SCORE' (text-xs font-black uppercase tracking-widest text-[#6f6f68]) + span '{score} / {MAX_SCORE}' (text-2xl font-black font-age text-[#14140f])
    - Progress bar: div h-3 w-full rounded-full border border-black bg-white overflow-hidden; inner div h-full rounded-full bg-[#c8f36a] transition-all duration-700; style={{ width: pct+'%' }}
    - Tips list (flex flex-col gap-2 mt-3): for each tip: flex items-center justify-between gap-2; left side: span emoji + span text with text-[#6f6f68] if inactive else text-[#14140f] font-black, line-through if done; right side: span className={tip.done ? 'bg-[#c8f36a]' : (tip.inactive ? 'bg-white text-[#6f6f68]' : 'bg-[#fff3a0]')+' px-2 py-0.5 text-xs font-black rounded-full border-2 border-black shadow-[2px_2px_0_rgba(20,20,15,0.9)]'} — text '+{tip.points}'

    Replace front/widgets/profile/ui/ProfileView.tsx (overwrite the stub completely):
    - 'use client'
    - Imports: Image from 'next/image'; useProfileQuery from '@/features/profile'; useState from 'react'; VibeScaleBar from '@/shared/ui/vibe-scale-bar'; RulesSection from '@/shared/ui/rules-section'; RoomieScoreCard from './RoomieScoreCard'; ProfileEditSheet from './ProfileEditSheet'
    - Hooks: const { data: profile, isLoading } = useProfileQuery(); const [editOpen, setEditOpen] = useState(false)
    - Loading state: return a pulse skeleton (div animate-pulse h-full bg-white/40 rounded-2xl)
    - If no profile: return null
    - TAG_COLORS = ['bg-[#c8f36a]', 'bg-[#a8d8ff]', 'bg-[#ffb8d4]']
    - VIBE_KEYS order: ['noiseLevel','cleanliness','sleepSchedule','socialLevel','workFromHome']
    - Layout: div className="flex flex-col gap-6 overflow-y-auto px-5 py-4 pb-[max(2rem,env(safe-area-inset-bottom))]"
    - Section header pattern: span className="text-xs font-black uppercase tracking-widest text-[#6f6f68]"
    - Header row: flex justify-between items-center; h3 'ПРОФИЛЬ' (section header style); button 'Изменить' onClick={() => setEditOpen(true)} className="text-sm font-black underline underline-offset-2 text-[#14140f]"
    - Photo + name row: flex items-center gap-4; photo div (h-24 w-24 rounded-2xl border-2 border-black shadow + Image or emoji placeholder); name+age span text-2xl font-black
    - Vibe tags: flex flex-wrap gap-1.5; profile.vibeTags.slice(0,3).map with TAG_COLORS chips
    - RoomieScoreCard: <RoomieScoreCard score={profile.roomieScore} hasPhotos={profile.photos.length>0} quizCompleted={profile.quizCompleted} dealbreakersSet={profile.onboardingStep>=4} />
    - "МОИ ПРЕДПОЧТЕНИЯ" section: budget string + districts comma-joined names
    - "ВАЙБ ДОМА" section: flex flex-col gap-2; {VIBE_KEYS.map(k => profile.lifestyleScales?.[k] != null && <VibeScaleBar key={k} scaleKey={k} value={profile.lifestyleScales![k]} />)}
    - "ПРАВИЛА" section: <RulesSection smokingOk={profile.smokingOk} petsOk={profile.petsOk} guestsPref={profile.guestsPref} />
    - At bottom: <ProfileEditSheet open={editOpen} profile={profile} onClose={() => setEditOpen(false)} />
  </action>
  <verify>
    <automated>cd front && npx tsc --noEmit 2>&1 | grep -i "profile" | head -15</automated>
  </verify>
  <done>ProfileView file replaces stub with full implementation. RoomieScoreCard file exists. TypeScript reports no errors for these files. Profile section headers match 03-UI-SPEC exactly.</done>
</task>

<task type="auto">
  <name>Task 2: Build ProfileEditSheet</name>
  <files>
    front/widgets/profile/ui/ProfileEditSheet.tsx
  </files>
  <read_first>
    - front/widgets/swipe-deck/ui/FilterSheet.tsx (full file — copy container, backdrop, handle, Escape handler pattern; Chip and Row components)
    - front/features/profile/index.ts (usePatchProfile export path)
    - front/shared/lib/api/profile.ts (MyProfile type, UpdateProfilePayload type)
    - front/shared/lib/api/client.ts (apiFetch for vibe-tags fetch and districts fetch)
    - front/shared/lib/telegram/index.ts (haptic export)
  </read_first>
  <action>
    Create front/widgets/profile/ui/ProfileEditSheet.tsx:
    'use client'
    Imports: useState, useEffect, useCallback from 'react'; MyProfile, UpdateProfilePayload from '@/shared/lib/api'; usePatchProfile from '@/features/profile'; apiFetch from '@/shared/lib/api/client'; haptic from '@/shared/lib/telegram'

    Props: interface ProfileEditSheetProps { open: boolean; profile: MyProfile; onClose: () => void; }

    Local state (initialized from profile on mount / when profile changes):
    - name: string
    - photoUrls: string[] (copy of profile.photos)
    - vibeTagIds: number[]
    - budgetMin: string (number as string for input)
    - budgetMax: string
    - districtIds: number[]
    - smokingOk: boolean | null
    - petsOk: boolean | null
    - guestsPref: 'rarely' | 'sometimes' | 'often' | null
    - newPhotoUrl: string (input state for URL-input)
    - availableDistricts: { id: number; name: string }[]
    - availableTags: { id: number; label: string }[]
    - errorMsg: string | null

    Initialize all local state from profile in useEffect([profile]).

    Data loads: useEffect on open=true: fetch districts via apiFetch('/geo/districts'), fetch tags via apiFetch('/vibe-tags').

    usePatchProfile hook: const patchMutation = usePatchProfile()

    Escape key handler: same pattern as FilterSheet.

    handleSave: useCallback → build UpdateProfilePayload from local state, call patchMutation.mutateAsync(payload), on success: haptic('success'), onClose(). On error: set errorMsg to 'Не сохранилось. Проверь соединение и попробуй ещё раз'.

    JSX structure (same sheet pattern as CandidateProfileSheet):
    - Backdrop: fixed inset-0 z-40 bg-black/40 backdrop-blur-sm; close on click
    - Sheet: fixed inset-x-0 bottom-0 z-50 flex flex-col max-h-[92dvh] rounded-t-3xl border-t-2 border-black bg-white shadow-[0_-4px_0_rgba(20,20,15,0.9)] transition-transform duration-300 ease-[cubic-bezier(0.34,1.2,0.64,1)]; translate-y-0/full
    - Handle + header row: handle div + flex justify-between: h3 'Редактировать профиль' (text-lg font-black) + button 'Отмена' (text-sm underline) onClick={onClose}
    - Scrollable content (flex-1 overflow-y-auto px-5 py-4 flex flex-col gap-5):

      ФОТО section:
      - Horizontal scroll of existing photos: flex gap-2 overflow-x-auto; each photo: div h-[64px] w-[52px] relative with Image + remove button (×, absolute top-0 right-0 small circle)
      - Add URL input: div flex gap-2; input type="text" value={newPhotoUrl} placeholder="https://..." className="flex-1 border-2 border-black rounded-xl px-4 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-[#c8f36a]"; button 'Добавить' onClick → if newPhotoUrl.trim(): setPhotoUrls(p => [...p, newPhotoUrl.trim()]); setNewPhotoUrl('')

      ИМЯ section:
      - input type="text" value={name} onChange={e => setName(e.target.value)} className="border-2 border-black rounded-xl px-4 py-3 text-sm font-black bg-white focus:outline-none focus:ring-2 focus:ring-[#c8f36a]"

      ВАЙБ-ТЕГИ section:
      - flex flex-wrap gap-2; for each availableTag: button chip; selected = vibeTagIds.includes(t.id); at limit (vibeTagIds.length >= 3) and not selected: add opacity-50 pointer-events-none; onClick: if selected → remove; else if < 3 → add
      - Selected chip: bg-[#c8f36a] border-2 border-black shadow-[2px_2px_0_rgba(20,20,15,0.9)]; unselected: bg-white border-2 border-black

      БЮДЖЕТ section:
      - flex gap-2; two inputs type="number" (от / до); same input style; placeholder 'От' / 'До'

      РАЙОНЫ section:
      - flex flex-wrap gap-2; chip per district; same toggle logic as FilterSheet

      КУРЕНИЕ / ПИТОМЦЫ / ГОСТИ sections (same Row+Chip pattern as FilterSheet; copy Row and Chip from FilterSheet or re-implement inline):
      - Smoking: null='Всё равно', false='🚭 Не курят', true='🚬 Курение ок'
      - Pets: null='Всё равно', true='🐾 Есть питомцы', false='🚫 Нет питомцев'
      - Guests: null='Всё равно', 'often'='Часто', 'sometimes'='Иногда', 'rarely'='Редко'

    - Footer (shrink-0 px-5 pb-[max(2rem,env(safe-area-inset-bottom))] pt-2 flex flex-col gap-2):
      - {errorMsg && <p className="text-xs text-[#ff6b85]">{errorMsg}</p>}
      - button 'Сохранить профиль' disabled={patchMutation.isPending} onClick={handleSave} className="w-full rounded-full border-2 border-black bg-accent py-3 font-black text-[#14140f] shadow-[3px_3px_0_rgba(20,20,15,0.9)] active:translate-x-[1px] active:translate-y-[1px] disabled:opacity-60"
  </action>
  <verify>
    <automated>cd front && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript clean. ProfileEditSheet exists at widgets/profile/ui/ProfileEditSheet.tsx. All form fields initialize from profile prop. handleSave calls patchMutation.mutateAsync with UpdateProfilePayload. Sheet uses correct z-50 and animation classes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ProfileEditSheet → PATCH /profile | User edits their own profile; form state sent as JSON body |
| ProfileView → GET /profile/me | User reads their own data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-13 | Tampering | ProfileEditSheet photo URL input | accept | URL stored as string — no server-side fetching of the URL in Phase 3; S3 upload deferred. URL validated by backend only via @IsString each |
| T-03-14 | Information Disclosure | ProfileView rendering own data | accept | useProfileQuery() only fetches /profile/me (authenticated, own data) |
| T-03-15 | Tampering | Budget fields (numeric strings) | mitigate | Parsed as Number() before sending to PATCH /profile; UpdateProfilePayload.budgetMin is number, not string |
| T-03-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
1. cd front && npx tsc --noEmit — no errors
2. ProfileView renders profile photo, name, age, vibe tags, RoomieScoreCard, preferences, VibeScaleBars, RulesSection
3. RoomieScoreCard shows score, progress bar width proportional to score/40, 4 tips with done/undone styling
4. 'Изменить' button opens ProfileEditSheet (editOpen state changes)
5. ProfileEditSheet: all fields initialize from profile prop; photo URL input adds to list; vibe tag limit enforced at 3
6. 'Сохранить профиль' calls PATCH /profile; sheet closes on success; error message appears on failure
7. haptic('success') fires on successful save
</verification>

<success_criteria>
- front/widgets/profile/ui/ProfileView.tsx replaces stub, renders full own profile
- front/widgets/profile/ui/RoomieScoreCard.tsx exists with score + 4 tips
- front/widgets/profile/ui/ProfileEditSheet.tsx exists with all edit fields and PATCH /profile mutation
- RoomieScoreCard phone tip always appears as inactive (undone, grey)
- ProfileEditSheet 'Сохранить' button is disabled while patchMutation.isPending
- TypeScript build clean
</success_criteria>

<output>
Create .planning/phases/03-discovery-profiles/03-06-SUMMARY.md when done
</output>
