---
phase: 03-discovery-profiles
plan: 03
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - "front/entities/profile/model/types.ts"
  - "front/shared/lib/api/feed.ts"
  - "front/shared/lib/api/profile.ts"
  - "front/shared/lib/api/index.ts"
  - "front/features/swipe-profile/model/use-feed-query.ts"
  - "front/features/swipe-profile/index.ts"
  - "front/features/profile/model/use-profile-query.ts"
  - "front/features/profile/index.ts"
  - "front/shared/ui/vibe-scale-bar/VibeScaleBar.tsx"
  - "front/shared/ui/vibe-scale-bar/index.ts"
  - "front/shared/ui/rules-section/RulesSection.tsx"
  - "front/shared/ui/rules-section/index.ts"
  - "front/shared/ui/match-reasons-list/MatchReasonsList.tsx"
  - "front/shared/ui/match-reasons-list/index.ts"
autonomous: true
requirements:
  - MATCH-06
  - MATCH-07
  - PROF-01
  - PROF-02
  - PROF-03
  - PROF-04

must_haves:
  truths:
    - "RoomieProfile type includes matchReasons, matchRisks, smokingOk, petsOk, guestsPref"
    - "useFeedQuery accepts FeedQueryParams and includes them in queryKey"
    - "useSwipeMutation accepts all 4 actions: like, pass, super_like, save"
    - "useProfileQuery fetches GET /profile/me and returns MyProfile shape"
    - "usePatchProfile calls PATCH /profile and invalidates profileKeys.me"
    - "VibeScaleBar renders a labeled progress bar with lime/peach/rose color based on value"
    - "RulesSection renders 3 dealbreaker chips (smokingOk, petsOk, guestsPref)"
    - "MatchReasonsList renders matchReasons as bullet list and matchRisks[0] as rose chip"
  artifacts:
    - path: "front/shared/ui/vibe-scale-bar/VibeScaleBar.tsx"
      provides: "VibeScaleBar component (scale key, value 0–1)"
    - path: "front/shared/ui/rules-section/RulesSection.tsx"
      provides: "RulesSection component (smokingOk, petsOk, guestsPref)"
    - path: "front/shared/ui/match-reasons-list/MatchReasonsList.tsx"
      provides: "MatchReasonsList component (matchReasons[], matchRisks?[])"
    - path: "front/features/swipe-profile/model/use-feed-query.ts"
      provides: "useFeedQuery(params?), useSwipeMutation() with all 4 actions"
    - path: "front/features/profile/model/use-profile-query.ts"
      provides: "useProfileQuery(), usePatchProfile()"
  key_links:
    - from: "front/features/swipe-profile/model/use-feed-query.ts"
      to: "front/shared/lib/api/feed.ts"
      via: "getFeed(params?) with FeedQueryParams"
      pattern: "getFeed.*FeedQueryParams"
    - from: "front/features/profile/model/use-profile-query.ts"
      to: "front/shared/lib/api/profile.ts"
      via: "getMe(), patchProfile(dto)"
      pattern: "getMe|patchProfile"
---

<objective>
Extend frontend type contracts (RoomieProfile, FeedCandidate), API layer (feed.ts, new profile.ts), React Query hooks (useFeedQuery with params, useSwipeMutation with all 4 actions, new useProfileQuery/usePatchProfile), and create the three reusable shared UI components (VibeScaleBar, RulesSection, MatchReasonsList) that both CandidateProfileSheet and ProfileView will consume.

Purpose: This plan is the data and UI contract layer that Wave 3 plans (03-04 CandidateProfileSheet, 03-05 ProfileView) depend on. Without correct types and hooks, those plans cannot proceed.
Output: Extended types, profile.ts API, profile hooks feature slice, 3 shared UI components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-discovery-profiles/03-CONTEXT.md
@.planning/phases/03-discovery-profiles/03-RESEARCH.md
@.planning/phases/03-discovery-profiles/03-UI-SPEC.md

<interfaces>
<!-- Key types to extend. Extracted from codebase. -->

From front/entities/profile/model/types.ts — current RoomieProfile (add 4 fields):
```typescript
export interface RoomieProfile {
  id: number; name: string; age?: number; scenario: string;
  budgetMin: number | null; budgetMax: number | null;
  photos: string[]; vibeTags: { id: number; label: string }[];
  districts: { id: number; name: string }[];
  lifestyleScales: { noiseLevel: number | null; cleanliness: number | null;
    sleepSchedule: number | null; socialLevel: number | null; workFromHome: number | null; } | null;
  matchScore: number;
  // Phase 3 — ADD:
  // smokingOk: boolean; petsOk: boolean; guestsPref: 'rarely' | 'sometimes' | 'often';
  // matchReasons: string[]; matchRisks?: string[];
}
```

From front/shared/lib/api/feed.ts — current FeedCandidate (add same 5 fields):
```typescript
export interface FeedCandidate {
  id: number; name: string; scenario: string;
  budgetMin: number | null; budgetMax: number | null;
  photos: string[]; vibeTags: ...; districts: ...; lifestyleScales: ...; matchScore: number;
  // Phase 3 — ADD: smokingOk, petsOk, guestsPref, matchReasons, matchRisks
}
export function getFeed(): Promise<FeedCandidate[]>
// Phase 3: getFeed(params?: FeedQueryParams): Promise<FeedCandidate[]>
```

From front/features/swipe-profile/model/use-feed-query.ts — current hook (no params):
```typescript
export const feedKeys = { all: ['feed'] as const };
export function useFeedQuery() {
  return useQuery({ queryKey: feedKeys.all, queryFn: getFeed, staleTime: 5 * 60 * 1000 });
}
export function useSwipeMutation() {
  // mutationFn: ({ targetId, action }: { targetId: number; action: 'like' | 'pass' }) => postSwipe(targetId, action)
  // Phase 3: extend action type to 'like' | 'pass' | 'super_like' | 'save'
  // Phase 3: also invalidate on 'super_like' (not just 'like')
}
```

From front/shared/lib/query/client.ts — QueryClient already configured with staleTime 5min, retry 1.

VibeScaleBar visual spec (from 03-UI-SPEC.md):
- Container: flex items-center gap-3
- Label: text-sm font-black text-[#14140f] w-[100px] shrink-0
- Track: flex-1 h-2 rounded-full bg-[#f0efe9] border-2 border-black overflow-hidden
- Fill: h-full rounded-full transition-all duration-500; color: value>=0.71 → bg-[#c8f36a]; value>=0.4 → bg-[#ffd7a8]; else → bg-[#ffd9e0]
- Percent: text-xs font-black text-[#14140f] w-8 text-right
- If value null → don't render the bar row

Scale keys and Russian labels + emoji (fixed order):
noiseLevel → '🔇 Тишина дома', cleanliness → '🧹 Чистота', sleepSchedule → '🌙 Режим сна', socialLevel → '💬 Общение', workFromHome → '💻 Дома / работа'

RulesSection visual spec (from 03-UI-SPEC.md):
- Container: flex flex-wrap gap-2
- Chip: rounded-full border-2 border-black px-3 py-2 text-sm font-black
- smokingOk=true → '🚬 Курение ок' bg-white text-[#6f6f68]; smokingOk=false → '🚭 Не курят' bg-[#c8f36a] text-[#14140f] shadow-[2px_2px_0_rgba(20,20,15,0.9)]
- petsOk=true → '🐾 Питомцы ок' bg-[#c8f36a] text-[#14140f] shadow; petsOk=false → '🚫 Без питомцев' bg-white text-[#6f6f68]
- guestsPref='often' → '🏠 Гости: часто' bg-white; 'sometimes' → '👌 Гости: иногда' bg-white; 'rarely' → '🤫 Гости: редко' bg-white

MatchReasonsList visual spec (from 03-UI-SPEC.md):
- Each reason: flex items-start gap-2 text-sm text-[#14140f]; prefix '✨'; font-normal leading-snug
- Risk chip: rounded-full border-2 border-black bg-[#ffd9e0] px-3 py-2 text-xs font-black text-[#14140f] shadow-[2px_2px_0_rgba(20,20,15,0.9)]; prefix '⚠️ '
- If matchReasons.length === 0: return null (don't render the section)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend types, API functions, and React Query hooks</name>
  <files>
    front/entities/profile/model/types.ts
    front/shared/lib/api/feed.ts
    front/shared/lib/api/profile.ts
    front/shared/lib/api/index.ts
    front/features/swipe-profile/model/use-feed-query.ts
    front/features/swipe-profile/index.ts
    front/features/profile/model/use-profile-query.ts
    front/features/profile/index.ts
  </files>
  <read_first>
    - front/entities/profile/model/types.ts (current RoomieProfile — extend it)
    - front/shared/lib/api/feed.ts (current FeedCandidate and getFeed/postSwipe — extend both)
    - front/shared/lib/api/index.ts (barrel — add profile exports)
    - front/features/swipe-profile/model/use-feed-query.ts (current hooks — extend)
    - front/features/swipe-profile/index.ts (barrel — confirm exports)
    - front/shared/lib/api/client.ts (apiFetch signature for use in profile.ts)
  </read_first>
  <action>
    In front/entities/profile/model/types.ts: add to RoomieProfile interface — smokingOk: boolean, petsOk: boolean, guestsPref: 'rarely' | 'sometimes' | 'often', matchReasons: string[], matchRisks?: string[]. Keep all existing fields unchanged.

    In front/shared/lib/api/feed.ts:
    - Add export interface FeedQueryParams { budgetMin?: number; budgetMax?: number; districtIds?: number[]; smokingOk?: boolean; petsOk?: boolean; guestsPref?: 'rarely' | 'sometimes' | 'often'; }
    - Add to FeedCandidate interface: smokingOk: boolean, petsOk: boolean, guestsPref: 'rarely' | 'sometimes' | 'often', matchReasons: string[], matchRisks?: string[]
    - Change getFeed signature to getFeed(params?: FeedQueryParams): Promise<FeedCandidate[]>. Build query string from params using URLSearchParams — append each non-undefined param; districtIds[] → append as multiple 'districtIds[]' entries. Call apiFetch('/feed?' + qs.toString()).
    - Change postSwipe action union to: 'like' | 'pass' | 'super_like' | 'save'

    Create front/shared/lib/api/profile.ts:
    - export interface MyProfile { id: number; name: string; age?: number; scenario: string; budgetMin: number|null; budgetMax: number|null; smokingOk: boolean; petsOk: boolean; guestsPref: 'rarely'|'sometimes'|'often'; photos: string[]; vibeTags: {id:number;label:string}[]; districts: {id:number;name:string}[]; lifestyleScales: {noiseLevel:number|null;cleanliness:number|null;sleepSchedule:number|null;socialLevel:number|null;workFromHome:number|null}|null; roomieScore: number; quizCompleted: boolean; onboardingStep: number; }
    - export interface UpdateProfilePayload { name?: string; photoUrls?: string[]; vibeTagIds?: number[]; budgetMin?: number; budgetMax?: number; districtIds?: number[]; smokingOk?: boolean; petsOk?: boolean; guestsPref?: 'rarely'|'sometimes'|'often'; }
    - export function getMe(): Promise<MyProfile> { return apiFetch<MyProfile>('/profile/me', { method: 'GET' }); }
    - export function patchProfile(dto: UpdateProfilePayload): Promise<MyProfile> { return apiFetch<MyProfile>('/profile', { method: 'PATCH', body: dto }); }

    In front/shared/lib/api/index.ts: add export * from './profile' (and export { FeedQueryParams } from './feed' if not already re-exported).

    In front/features/swipe-profile/model/use-feed-query.ts:
    - Update feedKeys: add filtered: (params: FeedQueryParams) => ['feed', params] as const
    - Update useFeedQuery to accept optional params?: FeedQueryParams. queryKey = params && Object.keys(params).length > 0 ? feedKeys.filtered(params) : feedKeys.all. queryFn = () => getFeed(params)
    - Update useSwipeMutation: change action type to 'like' | 'pass' | 'super_like' | 'save'. Invalidate queryKey feedKeys.all (with exact: false) on 'like' or 'super_like' actions — pass action does not invalidate.
    - Import FeedQueryParams from '@/shared/lib/api'

    In front/features/swipe-profile/index.ts: ensure FeedQueryParams is re-exported if needed by SwipeDeck.

    Create front/features/profile/model/use-profile-query.ts:
    - 'use client' directive
    - export const profileKeys = { me: ['profile', 'me'] as const }
    - export function useProfileQuery() { return useQuery({ queryKey: profileKeys.me, queryFn: getMe, staleTime: Infinity }); }
    - export function usePatchProfile() { const queryClient = useQueryClient(); return useMutation({ mutationFn: patchProfile, onSuccess: () => queryClient.invalidateQueries({ queryKey: profileKeys.me }) }); }
    - Imports: useMutation, useQuery, useQueryClient from '@tanstack/react-query'; getMe, patchProfile from '@/shared/lib/api'

    Create front/features/profile/index.ts: export { useProfileQuery, usePatchProfile, profileKeys } from './model/use-profile-query'
  </action>
  <verify>
    <automated>cd front && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>TypeScript compiler reports no type errors in the modified files. useFeedQuery(params) compiles with FeedQueryParams type. useSwipeMutation action union includes 'save'. useProfileQuery and usePatchProfile compile against MyProfile type.</done>
</task>

<task type="auto">
  <name>Task 2: Create shared UI components — VibeScaleBar, RulesSection, MatchReasonsList</name>
  <files>
    front/shared/ui/vibe-scale-bar/VibeScaleBar.tsx
    front/shared/ui/vibe-scale-bar/index.ts
    front/shared/ui/rules-section/RulesSection.tsx
    front/shared/ui/rules-section/index.ts
    front/shared/ui/match-reasons-list/MatchReasonsList.tsx
    front/shared/ui/match-reasons-list/index.ts
  </files>
  <read_first>
    - front/shared/ui/ (list existing files to understand current structure)
    - front/app/globals.css (color tokens: --text, --text-muted, accent, etc.)
    - front/widgets/swipe-deck/ui/FilterSheet.tsx (Chip component pattern for RulesSection chips)
  </read_first>
  <action>
    Create front/shared/ui/vibe-scale-bar/VibeScaleBar.tsx:
    - 'use client' directive
    - Props: interface VibeScaleBarProps { scaleKey: 'noiseLevel'|'cleanliness'|'sleepSchedule'|'socialLevel'|'workFromHome'; value: number|null; }
    - Constant SCALE_META: Record with each key → { label: string; icon: string } using the 5 entries from UI spec
    - If value is null, return null (don't render)
    - Fill color: value >= 0.71 → 'bg-[#c8f36a]'; value >= 0.4 → 'bg-[#ffd7a8]'; else → 'bg-[#ffd9e0]'
    - Container: div className="flex items-center gap-3"
    - Label: span className="text-sm font-black text-[#14140f] w-[100px] shrink-0" — shows icon + ' ' + label
    - Track: div className="flex-1 h-2 rounded-full bg-[#f0efe9] border-2 border-black overflow-hidden"
    - Fill: div className={fillColor + " h-full rounded-full transition-all duration-500"} style={{ width: Math.round(value * 100) + '%' }}
    - Percent: span className="text-xs font-black text-[#14140f] w-8 text-right" — {Math.round(value * 100)}%

    Create front/shared/ui/vibe-scale-bar/index.ts: export { VibeScaleBar } from './VibeScaleBar'

    Create front/shared/ui/rules-section/RulesSection.tsx:
    - 'use client' directive
    - Props: interface RulesSectionProps { smokingOk: boolean; petsOk: boolean; guestsPref: 'rarely'|'sometimes'|'often'; }
    - Container: div className="flex flex-wrap gap-2"
    - Each chip: span (not button — read-only) className="rounded-full border-2 border-black px-3 py-2 text-sm font-black" with conditional bg/text/shadow classes
    - smokingOk=false → bg-[#c8f36a] text-[#14140f] shadow-[2px_2px_0_rgba(20,20,15,0.9)]; true → bg-white text-[#6f6f68]
    - petsOk=true → bg-[#c8f36a] text-[#14140f] shadow; false → bg-white text-[#6f6f68]
    - guestsPref always → bg-white text-[#6f6f68] (neutral)
    - Labels per UI spec: smokingOk=false→'🚭 Не курят', true→'🚬 Курение ок'; petsOk=true→'🐾 Питомцы ок', false→'🚫 Без питомцев'; guestsPref='often'→'🏠 Гости: часто', 'sometimes'→'👌 Гости: иногда', 'rarely'→'🤫 Гости: редко'

    Create front/shared/ui/rules-section/index.ts: export { RulesSection } from './RulesSection'

    Create front/shared/ui/match-reasons-list/MatchReasonsList.tsx:
    - 'use client' directive
    - Props: interface MatchReasonsListProps { matchReasons: string[]; matchRisks?: string[]; }
    - If matchReasons.length === 0, return null
    - Each reason: div className="flex items-start gap-2 text-sm text-[#14140f]" with span '✨' and span className="font-normal leading-snug"
    - Risk chip (matchRisks?.[0] if present): span className="rounded-full border-2 border-black bg-[#ffd9e0] px-3 py-2 text-xs font-black text-[#14140f] shadow-[2px_2px_0_rgba(20,20,15,0.9)]" with prefix '⚠️ '
    - Outer container: div className="flex flex-col gap-2"

    Create front/shared/ui/match-reasons-list/index.ts: export { MatchReasonsList } from './MatchReasonsList'
  </action>
  <verify>
    <automated>cd front && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript reports no errors. VibeScaleBar, RulesSection, MatchReasonsList files exist at shared/ui/. Each has an index.ts barrel. Components are pure presentational (no API calls, no hooks).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend API layer → Backend | Feed and profile API calls include Bearer token; apiFetch handles injection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Information Disclosure | FeedQueryParams in queryKey | accept | queryKey is client-side only (React Query internal); params are the user's own filter preferences |
| T-03-08 | Tampering | postSwipe action type | mitigate | TypeScript union type 'like'|'pass'|'super_like'|'save' prevents invalid values at compile time; backend class-validator is the runtime gate |
| T-03-SC | Tampering | npm installs | accept | No new packages — @tanstack/react-query already installed |
</threat_model>

<verification>
1. cd front && npx tsc --noEmit — no type errors
2. RoomieProfile type includes matchReasons: string[], matchRisks?: string[], smokingOk, petsOk, guestsPref
3. useFeedQuery({budgetMin: 15000}) compiles with correct queryKey ['feed', {budgetMin: 15000}]
4. useSwipeMutation().mutate({targetId: 1, action: 'save'}) — TypeScript accepts 'save'
5. VibeScaleBar renders null for value=null, lime fill for value=0.8
6. RulesSection renders 3 chips with correct lime/white backgrounds
7. MatchReasonsList renders null for empty reasons array
</verification>

<success_criteria>
- RoomieProfile, FeedCandidate, FeedQueryParams types defined and exported
- MyProfile, UpdateProfilePayload types defined
- getMe(), patchProfile() API functions in shared/lib/api/profile.ts
- useFeedQuery(params?) and useSwipeMutation() with all 4 actions in features/swipe-profile/model/
- useProfileQuery() and usePatchProfile() in features/profile/model/
- VibeScaleBar, RulesSection, MatchReasonsList components in shared/ui/ with index.ts barrels
- TypeScript build clean (npx tsc --noEmit passes)
</success_criteria>

<output>
Create .planning/phases/03-discovery-profiles/03-03-SUMMARY.md when done
</output>
