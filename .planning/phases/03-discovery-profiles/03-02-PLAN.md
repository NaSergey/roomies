---
phase: 03-discovery-profiles
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - "roomies back/src/profile/profile.module.ts"
  - "roomies back/src/profile/profile.controller.ts"
  - "roomies back/src/profile/profile.service.ts"
  - "roomies back/src/profile/dto/update-profile.dto.ts"
  - "roomies back/src/app.module.ts"
  - "roomies back/src/profile/profile.service.spec.ts"
autonomous: true
requirements:
  - PROF-01
  - PROF-04

must_haves:
  truths:
    - "GET /profile/me returns user's name, photos, vibeTags, districts, budgetMin, budgetMax, smokingOk, petsOk, guestsPref, lifestyleScales, and computed roomieScore"
    - "roomieScore is computed live: +10 photos, +10 quizCompleted, +10 onboardingStep>=4, +10 isPhoneVerified (always 0 in Phase 3)"
    - "PATCH /profile updates name, photoUrls, vibeTagIds, budgetMin, budgetMax, districtIds, smokingOk, petsOk, guestsPref and recomputes roomieScore"
    - "Both endpoints require JWT authentication; userId comes from JWT, never from request body"
  artifacts:
    - path: "roomies back/src/profile/profile.module.ts"
      provides: "ProfileModule with PrismaModule import"
    - path: "roomies back/src/profile/profile.controller.ts"
      provides: "GET /profile/me, PATCH /profile with JwtAuthGuard"
    - path: "roomies back/src/profile/profile.service.ts"
      provides: "getMe(userId), updateProfile(userId, dto), computeRoomieScore()"
    - path: "roomies back/src/app.module.ts"
      provides: "ProfileModule registered in AppModule"
  key_links:
    - from: "roomies back/src/app.module.ts"
      to: "roomies back/src/profile/profile.module.ts"
      via: "imports: [ProfileModule]"
      pattern: "ProfileModule"
    - from: "roomies back/src/profile/profile.controller.ts"
      to: "roomies back/src/profile/profile.service.ts"
      via: "getMe(user.id), updateProfile(user.id, dto)"
      pattern: "getMe|updateProfile"
---

<objective>
Create a new ProfileModule with GET /profile/me and PATCH /profile endpoints, including live Roomie Score computation, following the established NestJS pattern from OnboardingModule.

Purpose: Enables the ProfileView frontend screen to fetch the user's own data and allow profile editing (PROF-01, PROF-04). No existing module covers these endpoints.
Output: ProfileModule, ProfileController, ProfileService, UpdateProfileDto, registered in AppModule.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-discovery-profiles/03-CONTEXT.md
@.planning/phases/03-discovery-profiles/03-RESEARCH.md

<interfaces>
<!-- Key patterns from existing modules. Extracted from codebase. -->

From roomies back/src/onboarding/onboarding.controller.ts — controller pattern to follow exactly:
```typescript
@ApiTags('onboarding')
@Controller('onboarding')
export class OnboardingController {
  constructor(private readonly onboarding: OnboardingService) {}

  @Get('status')
  @UseGuards(JwtAuthGuard)
  @ApiBearerAuth()
  getStatus(@CurrentUser() user: { id: number }) {
    return this.onboarding.getStatus(user.id);
  }

  @Patch('profile')
  @HttpCode(200)
  @UseGuards(JwtAuthGuard)
  @ApiBearerAuth()
  patchProfile(@CurrentUser() user: { id: number }, @Body() dto: ProfileDto) {
    return this.onboarding.saveProfile(user.id, dto);
  }
}
```

From roomies back/src/onboarding/onboarding.module.ts — module pattern (import AuthModule for JwtAuthGuard):
```typescript
@Module({
  imports: [PrismaModule, AuthModule],
  controllers: [OnboardingController],
  providers: [OnboardingService],
})
export class OnboardingModule {}
```

From roomies back/src/onboarding/onboarding.service.ts — profile save pattern (UserPhoto delete+create, vibeTags delete+create):
The existing saveProfile() deletes and recreates UserPhoto and UserVibeTag records. Follow the same pattern for PATCH /profile.

Roomie Score formula (from PROF-04 requirement + RESEARCH.md):
- +10 if user has at least 1 photo (photos.length > 0)
- +10 if quizCompleted = true
- +10 if onboardingStep >= 4 (proxy for dealbreakers set)
- +10 if isPhoneVerified = true (always 0 in Phase 3)
- Max = 30 in Phase 3 (phone not verifiable)
- Score is Int (not Float) — stored in User.roomieScore Int field

UserDistrict pattern (from onboarding.service.ts saveLocation):
- deleteMany where userId, then createMany with userId + districtId pairs.

telegramId is BigInt — never cast to number in any new code.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ProfileService and UpdateProfileDto</name>
  <files>
    roomies back/src/profile/profile.service.ts
    roomies back/src/profile/dto/update-profile.dto.ts
    roomies back/src/profile/profile.service.spec.ts
  </files>
  <read_first>
    - roomies back/src/onboarding/onboarding.service.ts (full file — pattern for saveProfile: photo delete+create, vibeTag delete+create, district delete+create)
    - roomies back/src/onboarding/dto/profile.dto.ts (existing ProfileDto fields as reference)
    - roomies back/src/onboarding/dto/dealbreakers.dto.ts (IsBoolean pattern for smokingOk/petsOk)
    - roomies back/prisma/schema.prisma (User fields: roomieScore Int, isPhoneVerified, quizCompleted, onboardingStep, photos relation, districts relation, vibeTags relation)
  </read_first>
  <action>
    Create roomies back/src/profile/dto/update-profile.dto.ts with UpdateProfileDto class. All fields are @IsOptional():
    - name: string (@IsString() @IsOptional())
    - photoUrls: string[] (@IsArray() @IsString({ each: true }) @IsOptional())
    - vibeTagIds: number[] (@IsArray() @IsInt({ each: true }) @IsOptional())
    - budgetMin: number (@IsInt() @IsOptional())
    - budgetMax: number (@IsInt() @IsOptional())
    - districtIds: number[] (@IsArray() @IsInt({ each: true }) @IsOptional())
    - smokingOk: boolean (@IsBoolean() @IsOptional())
    - petsOk: boolean (@IsBoolean() @IsOptional())
    - guestsPref: GuestsPreference (@IsEnum(GuestsPreference) @IsOptional()) — import from '@prisma/client'

    Create roomies back/src/profile/profile.service.ts with ProfileService class (@Injectable()):
    - constructor injects PrismaService
    - Private computeRoomieScore(user: { photos: {url:string}[]; quizCompleted: boolean; onboardingStep: number; isPhoneVerified: boolean }): number — returns +10 per criterion: photos.length>0, quizCompleted, onboardingStep>=4, isPhoneVerified
    - async getMe(userId: number): loads user with photos, vibeTags (tag.id + tag.label), districts (district.id + district.name), and all scalar fields; computes roomieScore live; returns shape: { id, name, age?, scenario, budgetMin, budgetMax, smokingOk, petsOk, guestsPref, photos: string[], vibeTags: {id,label}[], districts: {id,name}[], lifestyleScales: {noiseLevel,cleanliness,sleepSchedule,socialLevel,workFromHome}, roomieScore }. Use Number() cast on all Decimal lifestyle scale fields. Age computed same as feed.service.ts calculateAge function (copy private function or inline).
    - async updateProfile(userId: number, dto: UpdateProfileDto): updates scalar fields present in dto (name, budgetMin, budgetMax, smokingOk, petsOk, guestsPref) via prisma.user.update. If dto.photoUrls provided: delete all UserPhoto for userId, then createMany. If dto.vibeTagIds provided: deleteMany UserVibeTag, createMany. If dto.districtIds provided: deleteMany UserDistrict, createMany. After update, recompute and save roomieScore: load photos.count + quizCompleted + onboardingStep + isPhoneVerified, compute, prisma.user.update roomieScore. Returns updated getMe(userId).

    Create roomies back/src/profile/profile.service.spec.ts with unit tests:
    - computeRoomieScore: user with 1 photo + quizCompleted + onboardingStep=4 → score 30 (not 40, phone not verified)
    - computeRoomieScore: user with no photos, quizCompleted=false, onboardingStep=2 → score 0
    - getMe: returns roomieScore field (mock Prisma)
  </action>
  <verify>
    <automated>cd "roomies back" && npm run build 2>&1 | tail -5 && npm test -- --testPathPattern="profile.service" 2>&1 | tail -15</automated>
  </verify>
  <done>Build green. ProfileService compiles. Unit tests for computeRoomieScore pass. getMe and updateProfile signatures match the shape needed by frontend.</done>
</task>

<task type="auto">
  <name>Task 2: Create ProfileController and ProfileModule, register in AppModule</name>
  <files>
    roomies back/src/profile/profile.controller.ts
    roomies back/src/profile/profile.module.ts
    roomies back/src/app.module.ts
  </files>
  <read_first>
    - roomies back/src/onboarding/onboarding.controller.ts (full controller pattern — guard, decorators, CurrentUser)
    - roomies back/src/onboarding/onboarding.module.ts (module imports: PrismaModule, AuthModule)
    - roomies back/src/app.module.ts (current AppModule — add ProfileModule to imports)
    - roomies back/src/auth/current-user.decorator.ts (confirm @CurrentUser() decorator exists and returns { id: number })
  </read_first>
  <action>
    Create roomies back/src/profile/profile.controller.ts with ProfileController:
    - @ApiTags('profile') @Controller('profile')
    - @Get('me') @UseGuards(JwtAuthGuard) @ApiBearerAuth() getMe(@CurrentUser() user: { id: number }) → return this.profile.getMe(user.id)
    - @Patch() @HttpCode(200) @UseGuards(JwtAuthGuard) @ApiBearerAuth() updateProfile(@CurrentUser() user: { id: number }, @Body() dto: UpdateProfileDto) → return this.profile.updateProfile(user.id, dto)
    Note: the PATCH route is /profile (no sub-path), matching @Controller('profile') + @Patch().

    Create roomies back/src/profile/profile.module.ts:
    - @Module({ imports: [PrismaModule, AuthModule], controllers: [ProfileController], providers: [ProfileService] })

    In roomies back/src/app.module.ts:
    - Add import { ProfileModule } from './profile/profile.module'
    - Add ProfileModule to the imports array (after SwipeModule)
  </action>
  <verify>
    <automated>cd "roomies back" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Build green. GET /profile/me and PATCH /profile routes registered. curl -H "Authorization: Bearer $TOKEN" http://localhost:4000/profile/me returns user object with roomieScore field. PATCH /profile with body {"name": "Test"} returns updated profile.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP GET /profile/me | Authenticated user reads their own profile |
| HTTP PATCH /profile | Authenticated user writes their own profile |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Elevation of Privilege | GET /profile/me | mitigate | @UseGuards(JwtAuthGuard) + @CurrentUser() — userId from JWT only, no userId in query/path |
| T-03-05 | Tampering | PATCH /profile | mitigate | Same guard pattern; UpdateProfileDto with class-validator; userId from JWT, not body |
| T-03-06 | Information Disclosure | GET /profile/me | mitigate | Returns only current user's data (userId from JWT identity); no cross-user data exposure |
| T-03-SC | Tampering | npm installs | accept | No new packages in this plan |
</threat_model>

<verification>
1. cd "roomies back" && npm run build — no errors
2. npm test -- --testPathPattern="profile.service" — computeRoomieScore tests pass
3. Manual: curl -H "Authorization: Bearer $TOKEN" http://localhost:4000/profile/me returns { roomieScore, name, photos, vibeTags, districts, lifestyleScales, ... }
4. Manual: curl -X PATCH -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" -d '{"name":"Test Name"}' http://localhost:4000/profile returns updated profile
5. Manual: PATCH /profile with photoUrls replaces photos; vibeTagIds replaces tags; districtIds replaces districts
</verification>

<success_criteria>
- roomies back/src/profile/ directory contains profile.module.ts, profile.controller.ts, profile.service.ts, dto/update-profile.dto.ts
- ProfileModule registered in AppModule
- GET /profile/me returns roomieScore as integer (0–40 range)
- PATCH /profile with any subset of fields succeeds (200 response)
- Build green, profile.service.spec.ts tests pass
</success_criteria>

<output>
Create .planning/phases/03-discovery-profiles/03-02-SUMMARY.md when done
</output>
